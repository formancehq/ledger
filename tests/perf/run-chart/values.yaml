# Default values for k6-run chart
script: ""
spec:
  parallelism: 1
metadata:
  namespace: ""

# Ordered list of resources to create before the test.
# Each entry has a manifest and readyCondition.
resources: []

runner:
  env: {}
  # nodeSelector applied to k6 runner pods
  nodeSelector: {}
  # tolerations applied to k6 runner pods
  tolerations: []

archiveData: ""
