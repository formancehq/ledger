apiVersion: formance.com/v1beta1
kind: Settings
metadata:
  name: formance-settings
  namespace: formance-systems
spec:
  key: postgres.*.uri
  stacks:
    - "stack0"
  value: postgresql://ledger:ledger@postgres.formance-db.svc.cluster.local:5432?sslmode=disable
