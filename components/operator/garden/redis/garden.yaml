---
kind: Module
description: Redis
type: helm
name: redis
namespace: default
repo: https://charts.bitnami.com/bitnami
chart: redis
timeout: 3600
values:
  replica:
    replicaCount: 1
  auth:
    enabled: false
