issues:
  exclude-rules:
  - path: _test\.go
    linters:
    - typecheck

linters:
  enabled-all: true

run:
  timeout: 5m
  go: '1.18'
  skip-dirs:
    - apis/stack/v1beta1
    - apis/components/v1beta1
    - apis/benthos.components/v1beta1
    - apis/auth.components/v1beta1
