syntax = "proto3";

package bucket_commands;

import "ledger.proto";

option go_package = "github.com/formancehq/ledger-v3-poc/internal/raft/ledger";

// InsertLogCommand represents the data for an insert log command
message InsertLogCommand {
  ledger.Log log = 1;
}

