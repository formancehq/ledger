adminPassword: admin
datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - access: proxy
        editable: true
        isDefault: true
        name: VictoriaMetrics
        type: prometheus
        url: http://vm-victoria-metrics-single-server:8428
      - access: proxy
        editable: true
        name: Tempo
        type: tempo
        url: http://tempo:3200
      - access: proxy
        editable: true
        name: Loki
        type: loki
        url: http://loki:3100
      - access: proxy
        editable: true
        isDefault: false
        jsonData:
          allowedHosts:
            - http://ledger-exp:9000
        name: HTTP Infinity
        type: yesoreyeram-infinity-datasource
        uid: http-infinity
      - access: proxy
        editable: true
        isDefault: false
        jsonData:
          timeout: 10
        name: HTTP JSON API
        type: marcusolsson-json-datasource
        uid: http-json-api
        url: http://ledger-exp:9000
      - access: proxy
        editable: true
        isDefault: false
        name: Pyroscope
        type: grafana-pyroscope-datasource
        uid: pyroscope
        url: http://pyroscope:4040
deploymentStrategy:
  type: Recreate
grafana.ini:
  server:
    root_url: https://grafana-ledger-exp.staging.formance.cloud
    serve_from_sub_path: false
  snapshots:
    external_enabled: false
ingress:
  enabled: true
  hosts:
    - grafana-ledger-exp.staging.formance.cloud
  path: /
  pathType: Prefix
#  tls:
#    - hosts:
#        - grafana-ledger-exp.staging.formance.cloud
#      secretName: grafana-tls
persistence:
  accessModes:
    - ReadWriteOnce
  enabled: true
  finalizers:
    - kubernetes.io/pvc-protection
  size: 5Gi
  type: pvc
plugins:
  - marcusolsson-json-datasource
  - yesoreyeram-infinity-datasource
  - pgillich-tree-panel
  - grafana-pyroscope-app
resources:
  limits:
    cpu: 300m
    memory: 512Mi
  requests:
    cpu: 50m
    memory: 128Mi
service:
  type: ClusterIP
sidecar:
  dashboards:
    enabled: true
    label: grafana_dashboard
    labelValue: "1"
  datasources:
    enabled: true
