# Pyroscope Helm chart values
# https://github.com/grafana/pyroscope/tree/main/operations/pyroscope/helm/pyroscope

pyroscope:
  # Enable Pyroscope component
  enabled: true
  extraArgs:
    log.level: info
    # Data retention: 30 days (720h) for staging
    compactor.blocks-retention-period: "720h"
  resources:
    limits:
      cpu: 1000m
      memory: 1024Mi
    requests:
      cpu: 1000m
      memory: 1024Mi
  persistence:
    enabled: true
    size: 50Gi

# Disable other components we don't need
agent:
  enabled: false

minio:
  enabled: false

# Use single binary mode for simplicity
deploymentMode: monolithic
