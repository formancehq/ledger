image:
  pullSecrets:
    - zot
env:
  GRAFANA_URL: "https://grafana.ledger-exp.v2.formance.dev"
  GRAFANA_USER: "admin"
  GRAFANA_PASSWORD: "admin"
  # WATCH_NAMESPACE defaults to the release namespace
  SNAPSHOT_PER_NODE: "true"
  NODE_METRIC: "raft.node.lead"
  NODE_LABEL: "service.node_id"
  DATASOURCE_NAME: "VictoriaMetrics"
  SNAPSHOT_NAME_PREFIX: "k6-benchmark"
