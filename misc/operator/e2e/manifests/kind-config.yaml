kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # MinIO S3 API accessible on localhost:9000 for test verification
      - containerPort: 30900
        hostPort: 9000
        protocol: TCP
