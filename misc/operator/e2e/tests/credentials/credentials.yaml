apiVersion: ledger.formance.com/v1alpha1
kind: Credentials
metadata:
  name: e2e-credentials
spec:
  scopes:
    - "ledger:read"
    - "ledger:write"
  selector:
    matchLabels:
      tier: credentials-test
