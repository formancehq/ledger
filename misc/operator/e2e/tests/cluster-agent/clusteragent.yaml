apiVersion: ledger.formance.com/v1alpha1
kind: LedgerClusterAgent
metadata:
  name: e2e-agent
spec:
  scopes:
    - "ledger:read"
    - "ledger:write"
  selector:
    matchLabels:
      tier: agent-test
