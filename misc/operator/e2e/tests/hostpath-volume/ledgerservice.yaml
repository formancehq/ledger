apiVersion: ledger.formance.com/v1alpha1
kind: LedgerService
metadata:
  name: hp-test
spec:
  replicas: 1
  image:
    repository: alpine
    tag: "3"
  nodeSelector:
    formance.com/dedicated: ledger
  persistence:
    data:
      hostPath:
        path: /mnt/instance-store/data
