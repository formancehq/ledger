apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: invalid-clusterref
spec:
  timeouts:
    assert: 30s
  steps:
    - name: Backup with missing cluster should fail
      try:
        - apply:
            file: backup.yaml
        - assert:
            file: assert-failed.yaml
