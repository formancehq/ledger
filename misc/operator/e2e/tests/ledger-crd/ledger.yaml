apiVersion: ledger.formance.com/v1alpha1
kind: Ledger
metadata:
  name: test-ledger
spec:
  name: my-ledger
  clusterRef: crd-svc
  indexes:
    transaction:
      - reference
      - address
    account:
      - asset
    metadata:
      - target: account
        key: category
        type: string
