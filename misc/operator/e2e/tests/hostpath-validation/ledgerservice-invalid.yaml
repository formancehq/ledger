apiVersion: ledger.formance.com/v1alpha1
kind: LedgerService
metadata:
  name: hp-val
spec:
  replicas: 1
  image:
    repository: alpine
    tag: "3"
  persistence:
    data:
      storageClass: gp3
      hostPath:
        path: /mnt/instance-store/data
