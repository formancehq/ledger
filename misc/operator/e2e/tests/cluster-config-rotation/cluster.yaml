apiVersion: ledger.formance.com/v1alpha1
kind: Cluster
metadata:
  name: ccr-cluster
spec:
  replicas: 1
  image:
    repository: ledger
    tag: e2e
    pullPolicy: Never
  cache:
    rotationThreshold: 1000
  bloom:
    volumes:
      expectedKeys: 10000
      fpRate: "0.01"
