apiVersion: v1
kind: Service
metadata:
  name: {{ include "ledger-operator-ui.fullname" . }}
  labels:
    {{- include "ledger-operator-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "ledger-operator-ui.selectorLabels" . | nindent 4 }}
