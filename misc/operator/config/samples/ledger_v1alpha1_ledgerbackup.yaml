apiVersion: ledger.formance.com/v1alpha1
kind: LedgerBackup
metadata:
  name: my-backup
spec:
  serviceRef: my-ledger
  destination:
    driver: s3
    s3:
      bucket: my-ledger-backups
      region: us-east-1
  schedule:
    full: "0 2 * * 0"
    incremental: "0 * * * *"
