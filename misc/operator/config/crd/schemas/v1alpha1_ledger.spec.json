{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "LedgerCRDSpec defines the desired state of a Ledger.",
  "properties": {
    "clusterRef": {
      "description": "ClusterRef is the name of the Cluster in the same namespace\nthat provides the gRPC endpoint.",
      "type": "string"
    },
    "indexes": {
      "description": "Indexes declares the account/transaction indexes the operator maintains\non this ledger. This is especially useful for mirror ledgers, which are\nread-only and provisioned entirely through this CRD: it lets you keep the\nmirror queryable without hand-running `ledgerctl indexes create`.\n\nOwnership semantics:\n  - Field ABSENT (nil) =\u003e unmanaged. The operator never lists, creates,\n    or drops indexes on this ledger. This is the safe default so an\n    operator upgrade does not touch pre-existing indexes.\n  - Field PRESENT (even empty {}) =\u003e managed. The operator reconciles the\n    indexes it owns to match this spec: it creates the declared indexes\n    and drops indexes it previously created that are no longer declared.\n    It only ever drops indexes it created itself (tracked in\n    status.appliedIndexes) — externally-created indexes and index kinds\n    this CRD cannot express are left untouched. An empty {} therefore\n    means \"drop the indexes I previously managed\", not \"drop every index\".\n\nIndex maintenance is independent of ledger immutability: editing this\nfield never trips the SpecDrifted condition.",
      "properties": {
        "account": {
          "description": "Account lists the builtin account indexes to maintain. Only \"asset\"\n(which backs the `has asset` filter) is supported today.",
          "items": {
            "enum": [
              "asset"
            ],
            "type": "string"
          },
          "type": "array",
          "x-kubernetes-list-type": "set"
        },
        "metadata": {
          "description": "Metadata lists the metadata-key indexes to maintain on account or\ntransaction metadata. Each (target, key) pair must be unique.",
          "items": {
            "description": "MetadataIndexSpec declares one metadata-key index. Creating a metadata index\nrequires the metadata field to exist in the ledger schema first, so the\noperator declares the field type (via `ledgers set-metadata-type`) before\ncreating the index. The declared field type is reconciled too: changing Type\nre-declares the field, which the server treats as a schema change (it bumps\nthe index forward-encoding version and schedules a rewrite).",
            "properties": {
              "key": {
                "description": "Key is the metadata key name to index.",
                "maxLength": 256,
                "minLength": 1,
                "type": "string"
              },
              "target": {
                "description": "Target of the metadata key.",
                "enum": [
                  "account",
                  "transaction"
                ],
                "type": "string"
              },
              "type": {
                "description": "Type is the metadata field type declared before indexing.",
                "enum": [
                  "string",
                  "int64",
                  "bool",
                  "uint64",
                  "int8",
                  "int16",
                  "int32",
                  "uint8",
                  "uint16",
                  "uint32",
                  "datetime"
                ],
                "type": "string"
              }
            },
            "required": [
              "key",
              "target",
              "type"
            ],
            "type": "object"
          },
          "maxItems": 64,
          "type": "array",
          "x-kubernetes-validations": [
            {
              "message": "metadata index (target,key) pairs must be unique",
              "rule": "self.all(x, self.exists_one(y, y.target == x.target \u0026\u0026 y.key == x.key))"
            }
          ]
        },
        "transaction": {
          "description": "Transaction lists the builtin transaction indexes to maintain.",
          "items": {
            "enum": [
              "reference",
              "timestamp",
              "address",
              "sourceAddress",
              "destinationAddress",
              "insertedAt",
              "revertedAt"
            ],
            "type": "string"
          },
          "type": "array",
          "x-kubernetes-list-type": "set"
        }
      },
      "type": "object"
    },
    "mirrorSource": {
      "description": "MirrorSource configures the replication source when mode is \"mirror\".\nRequired when mode is \"mirror\".",
      "properties": {
        "batchSize": {
          "description": "BatchSize is the max number of log entries per replication batch (0 = default 100).",
          "format": "int32",
          "type": "integer"
        },
        "http": {
          "description": "HTTP configures replication via the Ledger v2 HTTP API.",
          "properties": {
            "baseUrl": {
              "description": "BaseURL is the URL of the source Ledger v2 API.",
              "type": "string"
            },
            "oauth2": {
              "description": "OAuth2 configures OAuth2 client credentials for authentication.",
              "properties": {
                "clientId": {
                  "description": "ClientID is the OAuth2 client ID.",
                  "type": "string"
                },
                "clientSecretFrom": {
                  "description": "ClientSecretFrom references a Kubernetes Secret containing the OAuth2 client secret.",
                  "properties": {
                    "key": {
                      "description": "Key is the key within the Secret.",
                      "type": "string"
                    },
                    "name": {
                      "description": "Name is the Secret name.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "key",
                    "name"
                  ],
                  "type": "object"
                },
                "scopes": {
                  "description": "Scopes is the list of OAuth2 scopes to request.",
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "tokenEndpoint": {
                  "description": "TokenEndpoint is the OAuth2 token endpoint URL.",
                  "type": "string"
                }
              },
              "required": [
                "clientId",
                "clientSecretFrom",
                "tokenEndpoint"
              ],
              "type": "object"
            }
          },
          "required": [
            "baseUrl"
          ],
          "type": "object"
        },
        "ledgerName": {
          "description": "LedgerName is the name of the ledger on the source system.\nDefaults to the Ledger's spec.name if not set.",
          "type": "string"
        },
        "postgres": {
          "description": "Postgres configures replication via direct PostgreSQL access.",
          "properties": {
            "awsIamAuth": {
              "description": "AWSIAMAuth enables AWS RDS IAM authentication: the mirror mints a\nshort-lived (15 min) SigV4 token per connection from the ambient AWS\ncredential chain on the ledger pod (IRSA, instance profile, env, profile).\nMutually exclusive with PasswordFrom.\n\nIn a typical EKS deployment, IRSA is wired by annotating the\nCluster ServiceAccount with eks.amazonaws.com/role-arn, e.g.:\n  kind: Cluster\n  spec:\n    serviceAccount:\n      annotations:\n        eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/mirror-rds-iam\nThe bound IAM role must allow rds-db:connect on the RDS db-user ARN\n(arn:aws:rds-db:REGION:ACCOUNT:dbuser:DB-RESOURCE-ID/USER). The role is\nshared across every mirror in the Cluster, so its policy must\ncover every RDS endpoint addressed by Ledger CRDs in that service.",
              "properties": {
                "assumeRoleArn": {
                  "description": "AssumeRoleArn is an optional STS role ARN to assume before minting the\nRDS IAM token. When set, the mirror calls sts:AssumeRole on this ARN\nusing the pod's ambient credentials and signs the RDS token with the\nassumed credentials. This decouples each mirror's IAM identity from the\npod's base role, so a single Cluster can mirror RDS instances\nacross multiple AWS accounts or tenants: the pod's base role only needs\nsts:AssumeRole on the listed targets (no direct rds-db:connect grant).\n\nWhen left empty, the pod's ambient credentials are used directly and\nmust hold rds-db:connect on the target db-user ARN.",
                  "minLength": 1,
                  "type": "string"
                },
                "region": {
                  "description": "Region is the AWS region of the RDS instance (e.g. \"eu-west-1\").\nRequired to sign the IAM authentication token (SigV4).",
                  "minLength": 1,
                  "type": "string"
                }
              },
              "required": [
                "region"
              ],
              "type": "object"
            },
            "database": {
              "description": "Database is the PostgreSQL database name.",
              "minLength": 1,
              "type": "string"
            },
            "host": {
              "description": "Host is the PostgreSQL endpoint hostname (e.g. RDS DB cluster endpoint).",
              "minLength": 1,
              "type": "string"
            },
            "passwordFrom": {
              "description": "PasswordFrom references a Kubernetes Secret containing the PostgreSQL password.\nMutually exclusive with AWSIAMAuth.",
              "properties": {
                "key": {
                  "description": "Key is the key within the Secret.",
                  "type": "string"
                },
                "name": {
                  "description": "Name is the Secret name.",
                  "type": "string"
                }
              },
              "required": [
                "key",
                "name"
              ],
              "type": "object"
            },
            "port": {
              "default": 5432,
              "description": "Port is the PostgreSQL port. Defaults to 5432.",
              "format": "int32",
              "maximum": 65535,
              "minimum": 1,
              "type": "integer"
            },
            "sslMode": {
              "default": "require",
              "description": "SSLMode is the libpq sslmode parameter (disable, allow, prefer, require,\nverify-ca, verify-full). Defaults to \"require\".",
              "enum": [
                "disable",
                "allow",
                "prefer",
                "require",
                "verify-ca",
                "verify-full"
              ],
              "type": "string"
            },
            "user": {
              "description": "User is the PostgreSQL user.",
              "minLength": 1,
              "type": "string"
            }
          },
          "required": [
            "database",
            "host",
            "user"
          ],
          "type": "object",
          "x-kubernetes-validations": [
            {
              "message": "exactly one of passwordFrom or awsIamAuth must be set",
              "rule": "has(self.passwordFrom) != has(self.awsIamAuth)"
            },
            {
              "message": "awsIamAuth requires sslMode in {require, verify-ca, verify-full}; non-TLS sslmodes would let the SigV4 bearer token travel in cleartext",
              "rule": "!has(self.awsIamAuth) || !has(self.sslMode) || self.sslMode in ['require','verify-ca','verify-full']"
            }
          ]
        },
        "rewriteRules": {
          "description": "RewriteRules are CEL rewrite rules applied, in order, to every mirror log\nentry as the mirror translates v2 source logs into v3 orders. Each rule can\nrename address segments, transform metadata, or drop transactions (see\nMirrorRewriteRule). Applies to both HTTP and Postgres sources.",
          "items": {
            "description": "MirrorRewriteRule transforms a mirror log entry during v2→v3 translation.\nA rule is scoped to exactly one variant of MirrorLogEntry.data. That scope\ndetermines the CEL type of `log` in the rule's predicate and the set of\nactions the rule can carry: `set_account_metadata` inside a\n`savedMetadata` rule is impossible to write, because the proto oneof\ndoesn't include it. When Stop is true and the rule matches (scope +\npredicate), no further rules are evaluated. Rewriting is a deterministic\ntranslation-time projection: the source v2 ledger is never modified.\n\nExactly one of the scope fields must be set.",
            "properties": {
              "anyVariant": {
                "description": "AnyVariantRule scopes to every variant. `log` is typed as MirrorLogEntry, so\nthe predicate must use `has(log.\u003cvariant\u003e)` guards. The action set is\nrestricted to operations that make sense on every variant.",
                "properties": {
                  "actions": {
                    "items": {
                      "properties": {
                        "drop": {
                          "type": "object"
                        },
                        "rewriteAddress": {
                          "properties": {
                            "pattern": {
                              "type": "string"
                            },
                            "replacement": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "pattern",
                            "replacement"
                          ],
                          "type": "object"
                        }
                      },
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "match": {
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "createdTransaction": {
                "description": "CreatedTransactionRule scopes to MirrorCreatedTransaction. Inside `match`,\n`log` is typed as the created-transaction proto — write `log.metadata[\"k\"]`\ndirectly, not `log.created_transaction.metadata[\"k\"]`.",
                "properties": {
                  "actions": {
                    "items": {
                      "properties": {
                        "deleteAccountMetadata": {
                          "properties": {
                            "account": {
                              "type": "string"
                            },
                            "key": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "account",
                            "key"
                          ],
                          "type": "object"
                        },
                        "deleteMetadata": {
                          "properties": {
                            "key": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "key"
                          ],
                          "type": "object"
                        },
                        "drop": {
                          "type": "object"
                        },
                        "rewriteAddress": {
                          "properties": {
                            "pattern": {
                              "type": "string"
                            },
                            "replacement": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "pattern",
                            "replacement"
                          ],
                          "type": "object"
                        },
                        "setAccountMetadata": {
                          "description": "SetAccountMetadataAction — see SetMetadataAction for the literal / valueExpr /\ntype semantics on the value field.",
                          "properties": {
                            "account": {
                              "type": "string"
                            },
                            "key": {
                              "type": "string"
                            },
                            "type": {
                              "type": "string"
                            },
                            "value": {
                              "type": "string"
                            },
                            "valueExpr": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "account",
                            "key"
                          ],
                          "type": "object"
                        },
                        "setAccountMetadataFromAddress": {
                          "description": "SetAccountMetadataFromAddressAction runs a single RE2 pattern over every\nposting address and, for each match, writes one or more metadata entries\non the matched account using group-capturing replacements.",
                          "properties": {
                            "pattern": {
                              "type": "string"
                            },
                            "replacements": {
                              "items": {
                                "properties": {
                                  "key": {
                                    "type": "string"
                                  },
                                  "replacement": {
                                    "type": "string"
                                  },
                                  "type": {
                                    "type": "string"
                                  }
                                },
                                "required": [
                                  "key",
                                  "replacement"
                                ],
                                "type": "object"
                              },
                              "type": "array"
                            }
                          },
                          "required": [
                            "pattern",
                            "replacements"
                          ],
                          "type": "object"
                        },
                        "setMetadata": {
                          "description": "SetMetadataAction sets a metadata key. `value` is a literal string; `valueExpr`\nis a CEL expression evaluated against the current variant at commit time\n(e.g. `log.reference`). Exactly one should be set; setting neither writes an\nempty string. Optional `type` coerces the produced string into a typed\nMetadataValue — one of \"string\" (default), \"int64\", \"bool\", \"uint64\",\n\"int8\"/\"int16\"/\"int32\", \"uint8\"/\"uint16\"/\"uint32\", \"datetime\".",
                          "properties": {
                            "key": {
                              "type": "string"
                            },
                            "type": {
                              "type": "string"
                            },
                            "value": {
                              "type": "string"
                            },
                            "valueExpr": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "key"
                          ],
                          "type": "object"
                        }
                      },
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "match": {
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "deletedMetadata": {
                "properties": {
                  "actions": {
                    "items": {
                      "properties": {
                        "drop": {
                          "type": "object"
                        },
                        "rewriteAddress": {
                          "properties": {
                            "pattern": {
                              "type": "string"
                            },
                            "replacement": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "pattern",
                            "replacement"
                          ],
                          "type": "object"
                        }
                      },
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "match": {
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "revertedTransaction": {
                "properties": {
                  "actions": {
                    "items": {
                      "properties": {
                        "deleteMetadata": {
                          "properties": {
                            "key": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "key"
                          ],
                          "type": "object"
                        },
                        "drop": {
                          "type": "object"
                        },
                        "rewriteAddress": {
                          "properties": {
                            "pattern": {
                              "type": "string"
                            },
                            "replacement": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "pattern",
                            "replacement"
                          ],
                          "type": "object"
                        },
                        "setMetadata": {
                          "description": "SetMetadataAction sets a metadata key. `value` is a literal string; `valueExpr`\nis a CEL expression evaluated against the current variant at commit time\n(e.g. `log.reference`). Exactly one should be set; setting neither writes an\nempty string. Optional `type` coerces the produced string into a typed\nMetadataValue — one of \"string\" (default), \"int64\", \"bool\", \"uint64\",\n\"int8\"/\"int16\"/\"int32\", \"uint8\"/\"uint16\"/\"uint32\", \"datetime\".",
                          "properties": {
                            "key": {
                              "type": "string"
                            },
                            "type": {
                              "type": "string"
                            },
                            "value": {
                              "type": "string"
                            },
                            "valueExpr": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "key"
                          ],
                          "type": "object"
                        }
                      },
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "match": {
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "savedMetadata": {
                "properties": {
                  "actions": {
                    "items": {
                      "properties": {
                        "deleteMetadata": {
                          "properties": {
                            "key": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "key"
                          ],
                          "type": "object"
                        },
                        "drop": {
                          "type": "object"
                        },
                        "rewriteAddress": {
                          "properties": {
                            "pattern": {
                              "type": "string"
                            },
                            "replacement": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "pattern",
                            "replacement"
                          ],
                          "type": "object"
                        },
                        "setMetadata": {
                          "description": "SetMetadataAction sets a metadata key. `value` is a literal string; `valueExpr`\nis a CEL expression evaluated against the current variant at commit time\n(e.g. `log.reference`). Exactly one should be set; setting neither writes an\nempty string. Optional `type` coerces the produced string into a typed\nMetadataValue — one of \"string\" (default), \"int64\", \"bool\", \"uint64\",\n\"int8\"/\"int16\"/\"int32\", \"uint8\"/\"uint16\"/\"uint32\", \"datetime\".",
                          "properties": {
                            "key": {
                              "type": "string"
                            },
                            "type": {
                              "type": "string"
                            },
                            "value": {
                              "type": "string"
                            },
                            "valueExpr": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "key"
                          ],
                          "type": "object"
                        }
                      },
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "match": {
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "stop": {
                "description": "Stop halts the rule chain once this rule matches (scope + predicate).",
                "type": "boolean"
              }
            },
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "mode": {
      "default": "normal",
      "description": "Mode is the ledger mode: \"normal\" or \"mirror\".\nA mirror ledger replicates data from a source system in read-only mode.",
      "enum": [
        "normal",
        "mirror"
      ],
      "type": "string"
    },
    "name": {
      "description": "Name is the ledger name to create/delete via gRPC.",
      "type": "string"
    }
  },
  "required": [
    "clusterRef",
    "name"
  ],
  "type": "object"
}
