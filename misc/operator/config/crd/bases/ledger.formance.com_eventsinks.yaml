---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: eventsinks.ledger.formance.com
spec:
  group: ledger.formance.com
  names:
    kind: EventSink
    listKind: EventSinkList
    plural: eventsinks
    shortNames:
    - esink
    singular: eventsink
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: Synced
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EventSink owns one Ledger runtime sink by its Kubernetes UID.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventSinkSpec declares one NATS sink in the referenced Ledger
              cluster.
            properties:
              batchDelayMs:
                description: BatchDelayMs is the maximum delay before a partial batch
                  is published.
                format: int64
                minimum: 0
                type: integer
              batchSize:
                description: BatchSize is the maximum number of events per batch.
                  Zero uses Ledger's default.
                format: int32
                maximum: 100000
                minimum: 0
                type: integer
              clusterRef:
                description: ClusterRef names a Cluster in the same namespace.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: clusterRef is immutable
                  rule: self == oldSelf
              eventTypes:
                description: EventTypes filters events. Empty selects all event types.
                items:
                  enum:
                  - COMMITTED_TRANSACTION
                  - REVERTED_TRANSACTION
                  - SAVED_METADATA
                  - DELETED_METADATA
                  - CREATED_LEDGER
                  - DELETED_LEDGER
                  - SKIPPED_ORDER
                  type: string
                type: array
                x-kubernetes-list-type: set
              format:
                default: json
                description: Format is the event serialization format.
                enum:
                - json
                - protobuf
                type: string
              nats:
                description: NATS configures the JetStream destination.
                properties:
                  topic:
                    description: Topic is the subject prefix.
                    minLength: 1
                    type: string
                  url:
                    description: URL is the NATS URL. Credentials must be referenced
                      from a Secret in a later API revision.
                    minLength: 1
                    pattern: ^nats://[^@]+$
                    type: string
                required:
                - topic
                - url
                type: object
            required:
            - clusterRef
            - nats
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              cursor:
                description: Cursor is the last published event sequence.
                format: int64
                type: integer
              error:
                description: Error is Ledger's current delivery error, if any.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
