{{- if .Values.archiveData }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k6-run.configmapName" . }}
  {{- if .Values.metadata.namespace }}
  namespace: {{ .Values.metadata.namespace }}
  {{- end }}
binaryData:
  {{ .Values.script }}.tar: {{ .Values.archiveData }}
{{- end }}
