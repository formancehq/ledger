{
  "name": "formance-stack-api",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.2",
    "openapi-merge-cli": "^1.3.1",
    "redoc-express": "^1.0.0"
  },
  "private": true,
  "scripts": {
    "cli": "openapi-merge-cli",
    "build": "yarn run cli",
    "build:all": "yarn run cli && task sdk:generate:all",
    "start": "node main.js"
  }
}
