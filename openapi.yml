openapi: 3.0.3
info:
  title: Ledger v3 API
  description: API for the Ledger v3, a distributed accounting ledger with Raft consensus
  version: 1.0.0
  contact:
    name: Formance
    url: https://formance.com

servers:
  - url: http://localhost:9000
    description: Local development server

security:
  - BearerAuth: []
  - {}

paths:
  /v3/:
    get:
      summary: List all ledgers
      description: Returns a list of all ledgers in the cluster. Requires `ledger:read` scope.
      operationId: listAllLedgers
      tags:
        - Ledgers
      security:
        - BearerAuth: []
        - {}
      responses:
        '200':
          description: List of ledgers retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAllLedgersResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /health:
    get:
      summary: Health check
      description: Returns the health status of the node. Returns 503 if the node is not connected to the cluster.
      operationId: health
      tags:
        - Health
      responses:
        '200':
          description: Node is healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /livez:
    get:
      summary: Liveness probe
      description: Returns 200 as long as the process is alive. Suitable for Kubernetes liveness probes.
      operationId: livez
      tags:
        - Health
      responses:
        '200':
          description: Process is alive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'

  /readyz:
    get:
      summary: Readiness probe
      description: |
        Returns 200 once the local Raft loop has started, regardless of whether
        a leader has been elected. Suitable for Kubernetes readiness probes and
        StatefulSet `OrderedReady` ordering: it is intentionally permissive so
        peer pods can be brought up during a cold start where quorum cannot
        form until they exist. Use `/clusterz` for the stricter
        cluster-availability signal.
      operationId: readyz
      tags:
        - Health
      responses:
        '200':
          description: Local Raft loop has started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /clusterz:
    get:
      summary: Cluster-availability probe
      description: |
        Returns 200 only when the node is part of a healthy cluster (Raft state
        healthy and a leader elected). Disk and clock skew gate writes (see
        admission), not cluster readiness. Returns 503 otherwise. Use this
        for external monitoring or any client that needs to wait until the node
        can actually serve cluster-dependent traffic. Kubernetes readiness
        probes should target `/readyz` instead — this endpoint MUST NOT gate
        StatefulSet ordering, otherwise the cluster cannot recover from a
        full-kill cold start.
      operationId: clusterz
      tags:
        - Health
      responses:
        '200':
          description: Node is part of a healthy cluster
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /_info:
    get:
      summary: Get server build information
      description: Returns the server build metadata (version, commit, build date, Go runtime version). Unauthenticated.
      operationId: info
      tags:
        - Health
      responses:
        '200':
          description: Server build information
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServerInfo'

  /v3/{ledgerName}:
    get:
      summary: Get a ledger
      description: Retrieves a ledger by its name. Requires `ledger:read` scope.
      operationId: getLedger
      tags:
        - Ledgers
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      responses:
        '200':
          description: Ledger retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetLedgerResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    post:
      summary: Create a new ledger
      description: |
        Creates a new ledger with the specified name. Storage is configured at the server level.
        Each ledger has its own Raft group for data consistency. Requires `ledger:write` scope.

        The ledger name `_` is reserved: all system / non-ledger routes live
        under the `/v3/_/…` segment (e.g. `/v3/_/audit-entries`), so a ledger
        named `_` would be unreachable over REST. Creating it is rejected with a
        validation error.
      operationId: createLedger
      tags:
        - Ledgers
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLedgerRequest'
      responses:
        '201':
          description: Ledger created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateLedgerResponse'
          links:
            GetCreatedLedger:
              operationId: getLedger
              parameters:
                ledgerName: '$response.body#/data/name'
            DeleteCreatedLedger:
              operationId: deleteLedger
              parameters:
                ledgerName: '$response.body#/data/name'
            GetLedgerStats:
              operationId: getLedgerStats
              parameters:
                ledgerName: '$response.body#/data/name'
            CreateTransactionInLedger:
              operationId: createTransaction
              parameters:
                ledgerName: '$response.body#/data/name'
            ListLedgerAccounts:
              operationId: listAccounts
              parameters:
                ledgerName: '$response.body#/data/name'
            ListLedgerLogs:
              operationId: listLedgerLogs
              parameters:
                ledgerName: '$response.body#/data/name'
            SaveNumscriptInLedger:
              operationId: saveNumscript
              parameters:
                ledgerName: '$response.body#/data/name'
            BulkInLedger:
              operationId: bulkOperations
              parameters:
                ledgerName: '$response.body#/data/name'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    delete:
      summary: Delete a ledger
      description: Deletes a ledger by its name. This operation removes the ledger and its associated Raft group from the cluster. Requires `ledger:write` scope.
      operationId: deleteLedger
      tags:
        - Ledgers
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Ledger deleted successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/stats:
    get:
      summary: Get ledger statistics
      description: >-
        Returns aggregate usage statistics (transaction, volume, reference, posting, log, revert
        and Numscript-execution counts, plus ephemeral-evicted and transient-used volume tallies)
        for a ledger. Requires `ledger:read` scope.
        Note: for checkpoint-scoped (historical) reads, only `transactionCount` and `logCount` are
        checkpoint-consistent; the usage-derived counters (`postingCount`, `revertCount`,
        `numscriptExecutionCount`, `referenceCount`, `ephemeralEvictedCount`, `transientUsedCount`,
        `volumeCount`) are sourced from the live usagestore projection, which is not checkpoint-aware,
        and are returned as `0` at a checkpoint rather than a live value — treat `0` on a checkpoint
        read as "not available at this checkpoint".
      operationId: getLedgerStats
      tags:
        - Ledgers
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      responses:
        '200':
          description: Ledger statistics retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetLedgerStatsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/logs:
    get:
      summary: List logs for a ledger
      description: |
        Lists log entries for a specific ledger, ordered by ledger-local log ID ascending.
        Requires the builtin `LOG` index to be enabled on the ledger via `CreateIndex`.
        Supports cursor pagination via the `after` query parameter. Requires `ledger:OpsRead` scope.
      operationId: listLedgerLogs
      tags:
        - Logs
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: pageSize
          in: query
          schema:
            type: integer
            format: uint32
            minimum: 0
            default: 0
          description: Maximum number of log entries to return (0 = no limit)
        - name: after
          in: query
          schema:
            type: integer
            format: uint64
            minimum: 0
          description: Ledger-local log ID to start after (exclusive, for cursor pagination)
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
          description: |
            Filter logs with date >= startDate (inclusive, RFC3339 format).
            Must be on or after the Unix epoch (1970-01-01T00:00:00Z); earlier
            dates are rejected with a 400.
            Requires the builtin `LOG_DATE` index to be enabled on the ledger.
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
          description: |
            Filter logs with date < endDate (exclusive, RFC3339 format).
            Must be on or after the Unix epoch (1970-01-01T00:00:00Z); earlier
            dates are rejected with a 400.
            Requires the builtin `LOG_DATE` index to be enabled on the ledger.
        - $ref: '#/components/parameters/ListFilter'
      responses:
        '200':
          description: Logs retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  description: System log entry (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/_/audit-entries:
    get:
      summary: List audit entries
      description: |
        Lists audit trail entries (one per proposal, success or failure) across
        the whole bucket, ordered by audit sequence ascending. Audit reads are
        not ledger-scoped in the path — a single proposal can touch several
        ledgers — so ledger scope, outcome and other dimensions are expressed
        through the `filter` query parameter.

        This endpoint always performs a live, best-effort read. Unlike the gRPC
        `ListAuditEntries` surface, it does not expose the read-consistency
        options (pinned checkpoint reads, or a consistency-bound wait on the
        audit index): filtered reads are served by an asynchronous secondary
        index and may transiently omit very recent entries that are not yet
        indexed. A client that needs a pinned or consistency-bounded audit read
        must use the gRPC surface.

        Requires `ledger:AuditRead` scope.
      operationId: listAuditEntries
      tags:
        - Audit
      security:
        - BearerAuth: []
        - {}
      parameters:
        - name: pageSize
          in: query
          schema:
            type: integer
            format: uint32
            minimum: 0
            default: 0
          description: Maximum number of audit entries to return (0 = server default, capped at 1000)
        - name: after
          in: query
          schema:
            type: integer
            format: uint64
            minimum: 0
          description: Audit sequence to start after (exclusive, opaque cursor for pagination)
        - name: reverse
          in: query
          schema:
            type: boolean
            default: false
          description: Iterate newest-first (descending sequence) when true
        - name: filter
          in: query
          schema:
            type: string
          description: |
            A filter, decoded by the same shared dual-format decoder every list
            endpoint uses (see the `ListFilter` parameter). In practice audit
            filters use the **textual** representation, restricted to bare
            audit fields (`outcome`, `ledger`, `seq`, `proposal_id`,
            `timestamp`, `log_seq`, `caller_subject`, `order_type`). These are
            written without any prefix and are resolved against the audit query
            target (EN-1549 — this replaced the old `audit[...]` namespaced
            syntax; it is a breaking change with no backward compatibility):
              - `outcome == failure`
              - `ledger == main`
              - `order_type in (create_transaction, revert_transaction)`
              - `seq between 1000 and 2000`
              - `timestamp >= "2023-11-14T22:13:20Z"`
            Supported fields: `seq`, `proposal_id`, `log_seq` (numeric);
            `timestamp` (RFC3339 or unix microseconds); `outcome`
            (success|failure), `ledger`, `caller_subject`, `order_type`
            (string). Bare audit fields are resolved against the audit query
            target: `timestamp` and `ledger` collide with the transaction
            `timestamp` field and the top-level `ledger` condition, so on the
            audit target they resolve to the audit condition (and only there —
            hence audit fields are valid on this endpoint only). Audit
            conditions have NO structured JSON QueryFilter representation, so
            the textual form is the canonical one here; a JSON QueryFilter
            cannot carry an audit condition and is rejected.
      responses:
        '200':
          description: List of audit entries
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AuditEntry'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/_/audit-entries/{sequence}:
    get:
      summary: Get an audit entry
      description: |
        Returns a single audit entry by its global sequence, with per-order
        `items` populated (the list endpoint omits `items` for compactness).
        Requires `ledger:AuditRead` scope.
      operationId: getAuditEntry
      tags:
        - Audit
      security:
        - BearerAuth: []
        - {}
      parameters:
        - name: sequence
          in: path
          required: true
          schema:
            type: integer
            format: uint64
            minimum: 0
          description: Audit entry sequence
      responses:
        '200':
          description: The audit entry
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AuditEntry'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/promote:
    post:
      summary: Promote a mirror ledger
      description: |
        Promotes a mirror-mode ledger to normal mode. After promotion, the mirror sync
        worker is stopped and the ledger accepts direct write operations. Requires `ledger:write` scope.
      operationId: promoteLedger
      tags:
        - Ledgers
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      responses:
        '201':
          description: Ledger promoted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromoteLedgerResponse'
        '400':
          description: Ledger is not in mirror mode
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/transactions:
    post:
      summary: Create a new transaction
      description: Creates a new transaction in the specified ledger with the specified postings. Requires `ledger:write` scope.
      operationId: createTransaction
      tags:
        - Transactions
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTransactionRequest'
      responses:
        '201':
          description: Transaction created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateTransactionResponse'
          links:
            GetCreatedTransaction:
              operationId: getTransaction
              parameters:
                ledgerName: '$request.path.ledgerName'
                transactionId: '$response.body#/data/transaction/id'
            RevertCreatedTransaction:
              operationId: revertTransaction
              parameters:
                ledgerName: '$request.path.ledgerName'
                transactionId: '$response.body#/data/transaction/id'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Conflict (concurrent modification, or reference already claimed by another transaction). Callers that need continue-on-failure semantics should use the bulk endpoint with a per-entry `skippableReasons` list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    get:
      summary: List transactions for a ledger
      description: |
        Lists transactions for a specific ledger. The default order is
        newest-first (descending transaction id); pass `reverse=true` to
        get oldest-first (ascending). Supports cursor pagination via the
        `after` query parameter, an optional date range (`startDate`/`endDate`),
        and the generic `filter` parameter (e.g. reference selection via
        `filter={"$match":{"reference":"..."}}`, URL-encoded). Requires
        `ledger:TransactionRead` scope.

        This is a live, best-effort read of the current committed state.
        Unlike the gRPC API, it does not accept read-consistency options
        (`checkpointId` / `minLogSequence`) to pin the read to a specific
        applied index — use gRPC for consistency-bounded / checkpoint-pinned
        reads. (Tracked follow-up, same carve-out as the audit reads.)
      operationId: listLedgerTransactions
      tags:
        - Transactions
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: pageSize
          in: query
          schema:
            type: integer
            format: uint32
            minimum: 0
            default: 0
        - name: after
          in: query
          schema:
            type: integer
            format: uint64
            minimum: 0
          description: Transaction id to start after (exclusive, for cursor pagination).
        - name: reverse
          in: query
          schema:
            type: boolean
            default: false
          description: |
            Reverse the default order. Default (`false`) returns
            newest-first (descending); `true` returns oldest-first
            (ascending). Mirrors the gRPC `ListTransactions` reverse
            flag semantic.
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
          description: |
            Filter transactions with timestamp >= startDate (inclusive, RFC3339 format).
            Requires the builtin `TX_BUILTIN_INDEX_TIMESTAMP` index to be enabled on the ledger.
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
          description: |
            Filter transactions with timestamp < endDate (exclusive, RFC3339 format).
            Requires the builtin `TX_BUILTIN_INDEX_TIMESTAMP` index to be enabled on the ledger.
        - $ref: '#/components/parameters/ListFilter'
      responses:
        '200':
          description: Transactions retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: Transaction (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/transactions/{transactionId}:
    get:
      summary: Get a transaction
      description: Retrieves a transaction by its ID from the specified ledger. Requires `ledger:read` scope.
      operationId: getTransaction
      tags:
        - Transactions
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/TransactionId'
      responses:
        '200':
          description: Transaction retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetTransactionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/accounts:
    get:
      summary: List accounts
      description: |
        Lists accounts in a ledger in alphabetical order with cursor
        pagination and an optional generic `filter` (e.g. address-prefix
        selection via `filter=address ^= "users:"`). Requires `ledger:read`
        scope.
      operationId: listAccounts
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: pageSize
          in: query
          description: Maximum number of accounts to return (0 = no limit)
          schema:
            type: integer
            format: int32
        - name: after
          in: query
          description: Account address to start after (for cursor pagination, exclusive)
          schema:
            type: string
        - $ref: '#/components/parameters/ListFilter'
      responses:
        '200':
          description: List of accounts
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Account'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/accounts/{address}:
    get:
      summary: Get an account
      description: Retrieves account information including metadata for a specific account address. Requires `ledger:read` scope.
      operationId: getAccount
      tags:
        - Accounts
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/AccountAddress'
        - name: collapseColors
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: |
            When true, every colored bucket of the same asset is summed
            into a single AccountVolume entry with color = "". By default
            each (asset, color) tuple is returned as its own entry.
      responses:
        '200':
          description: Account retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetAccountResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/volumes:
    get:
      summary: Aggregate volumes
      description: |
        Returns aggregated volumes (input/output/balance per asset) across all accounts in the ledger.
        Supports filtering by account address prefix, grouping by account prefixes, and max-precision merging.
        Requires `ledger:AccountRead` scope.
      operationId: aggregateVolumes
      tags:
        - Volumes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: prefix
          in: query
          description: |
            Filter accounts whose address starts with this raw byte-level
            prefix (e.g. `users:` matches `users:alice`, `users:bob`, ...).
            Matching is not path-segment aware: `acc:1` also matches `acc:10`,
            `acc:11`, etc. Do not use for exact-address lookups.
          schema:
            type: string
        - name: useMaxPrecision
          in: query
          description: Merge assets with the same base at the highest observed precision
          schema:
            type: boolean
            default: false
        - name: groupByPrefixes
          in: query
          description: Comma-separated list of account prefixes to group volumes by
          schema:
            type: string
        - name: collapseColors
          in: query
          description: |
            When true, every colored bucket of the same (asset, precision) is
            summed and the resulting AggregatedVolume entry carries color = "".
            By default the response is segregated per (asset, color).
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: Aggregated volumes retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AggregateVolumesResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/metadata:
    post:
      summary: Save ledger metadata
      description: Saves metadata key-value pairs on the ledger itself. Requires `ledger:write` scope.
      operationId: saveLedgerMetadata
      tags:
        - Ledgers
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                $ref: '#/components/schemas/MetadataValue'
              description: Metadata key-value pairs (supports typed values)
              example:
                environment: production
                team: payments
                region: eu-west-1
      responses:
        '204':
          description: Ledger metadata saved successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/metadata/{key}:
    delete:
      summary: Delete ledger metadata
      description: Deletes a metadata key from the ledger. Requires `ledger:write` scope.
      operationId: deleteLedgerMetadata
      tags:
        - Ledgers
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/MetadataKey'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Ledger metadata deleted successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/accounts/{address}/metadata:
    post:
      summary: Save account metadata
      description: Saves metadata for a specific account in the ledger. Requires `ledger:write` scope.
      operationId: saveAccountMetadata
      tags:
        - Accounts
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/AccountAddress'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                $ref: '#/components/schemas/MetadataValue'
              description: Metadata key-value pairs (supports typed values)
              example:
                account_type: asset
                label: Bank Account
                active: true
                balance_threshold: 1000
      responses:
        '204':
          description: Account metadata saved successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/transactions/{transactionId}/metadata:
    post:
      summary: Save transaction metadata
      description: Saves metadata for a specific transaction in the ledger. Requires `ledger:write` scope.
      operationId: saveTransactionMetadata
      tags:
        - Transactions
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/TransactionId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                $ref: '#/components/schemas/MetadataValue'
              description: Metadata key-value pairs (supports typed values)
              example:
                reason: correction
                source: support
                priority: 1
      responses:
        '204':
          description: Transaction metadata saved successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/accounts/{address}/metadata/{key}:
    delete:
      summary: Delete account metadata
      description: Deletes a metadata key for a specific account in the ledger. Requires `ledger:write` scope.
      operationId: deleteAccountMetadata
      tags:
        - Accounts
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/AccountAddress'
        - $ref: '#/components/parameters/MetadataKey'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Account metadata deleted successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/transactions/{transactionId}/metadata/{key}:
    delete:
      summary: Delete transaction metadata
      description: Deletes a metadata key for a specific transaction in the ledger. Requires `ledger:write` scope.
      operationId: deleteTransactionMetadata
      tags:
        - Transactions
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/TransactionId'
        - $ref: '#/components/parameters/MetadataKey'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Transaction metadata deleted successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/transactions/{transactionId}/revert:
    post:
      summary: Revert a transaction
      description: Reverts a transaction by creating a reverse transaction that undoes its effects. Requires `ledger:write` scope.
      operationId: revertTransaction
      tags:
        - Transactions
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/TransactionId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RevertTransactionRequestBody'
      responses:
        '201':
          description: Transaction reverted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RevertedTransactionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Conflict (e.g. transaction already reverted)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/metadata-schema:
    get:
      summary: Get metadata schema status
      description: Returns the metadata schema for a ledger, including per-field type declarations and conversion status. Requires `ledger:read` scope.
      operationId: getMetadataSchemaStatus
      tags:
        - Metadata Schema
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      responses:
        '200':
          description: Metadata schema status retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetadataSchemaStatusResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/indexes/{canonicalId}/inspect:
    get:
      summary: Inspect a metadata index
      description: |
        Scans a metadata index and returns distinct values, facets (value + count),
        or a summary (cardinality, min/max, existence counts). The index must be
        in READY state. Only metadata indexes are inspectable (builtin indexes
        return 400). Requires `ledger:read` scope.
      operationId: inspectIndex
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: canonicalId
          in: path
          required: true
          description: |
            Canonical form of the target IndexID, e.g.
            `metadata:TARGET_TYPE_ACCOUNT:color`. Must resolve to a metadata
            index; builtin indexes (`tx_builtin:...`, etc.) are rejected.
          schema:
            type: string
        - name: mode
          in: query
          description: Inspection mode
          schema:
            type: string
            enum: [summary, distinctValues, facets]
            default: summary
        - name: pageSize
          in: query
          description: Page size for distinctValues/facets modes
          schema:
            type: integer
            minimum: 1
            maximum: 10000
        - name: cursor
          in: query
          description: Pagination cursor from previous response
          schema:
            type: string
      responses:
        '200':
          description: Index inspection result
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    oneOf:
                      - $ref: '#/components/schemas/InspectSummaryResponse'
                      - $ref: '#/components/schemas/InspectDistinctValuesResponse'
                      - $ref: '#/components/schemas/InspectFacetsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/analyze-accounts:
    get:
      summary: Analyze accounts and suggest a Chart of Accounts
      description: |
        Scans all accounts in a ledger, discovers address patterns, and returns a
        suggested Chart of Accounts. Useful after a mirror import to understand
        account structure. Requires `ledger:read` scope.
      operationId: analyzeAccounts
      tags:
        - Accounts
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: variableThreshold
          in: query
          description: Max distinct children before classifying as variable (0 = default 10)
          required: false
          schema:
            type: integer
            format: int32
            default: 0
      responses:
        '200':
          description: Analysis completed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalyzeAccountsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/analyze-transactions:
    get:
      summary: Analyze transaction flow patterns
      description: |
        Scans all transactions in a ledger, discovers flow patterns by normalizing
        account addresses, and returns statistics per flow type including temporal
        distribution and volume metrics. Requires `ledger:read` scope.
      operationId: analyzeTransactions
      tags:
        - Transactions
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: variableThreshold
          in: query
          description: Max distinct children before classifying as variable (0 = default 10)
          required: false
          schema:
            type: integer
            format: int32
            default: 0
      responses:
        '200':
          description: Analysis completed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalyzeTransactionsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/metadata-schema/{targetType}/{key}:
    put:
      summary: Set metadata field type
      description: |
        Declares or changes the expected type for a metadata key on a ledger.
        Once set, all new metadata values for this key are converted to the declared type.
        Existing values are converted in the background. Requires `ledger:write` scope.
      operationId: setMetadataFieldType
      tags:
        - Metadata Schema
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/TargetType'
        - name: key
          in: path
          required: true
          schema:
            type: string
          description: Metadata key name
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetMetadataTypeRequest'
      responses:
        '204':
          description: Metadata field type set successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    delete:
      summary: Remove metadata field type
      description: |
        Removes the type declaration for a metadata key on a ledger.
        Existing typed values are converted back to strings. Requires `ledger:write` scope.
      operationId: removeMetadataFieldType
      tags:
        - Metadata Schema
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/TargetType'
        - name: key
          in: path
          required: true
          schema:
            type: string
          description: Metadata key name
      responses:
        '204':
          description: Metadata field type removed successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/numscripts:
    get:
      summary: List all numscripts
      description: Returns every numscript for a ledger at its greatest stored version.
      operationId: listNumscripts
      tags:
        - Numscript Library
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      responses:
        '200':
          description: List of numscripts retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNumscriptsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/numscripts/{name}:
    get:
      summary: Get a numscript
      description: |
        Retrieves a numscript by name from a ledger. Use the `version` query parameter to get a specific
        semver version. If omitted or empty, the latest version is returned.
      operationId: getNumscript
      tags:
        - Numscript Library
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the numscript
        - name: version
          in: query
          required: false
          schema:
            type: string
          description: |
            Semver version to retrieve (e.g. "1.0.0"). If omitted or empty, the latest version is returned.
      responses:
        '200':
          description: Numscript retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetNumscriptResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Numscript not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    put:
      summary: Save a numscript
      description: |
        Appends an immutable version to a ledger's numscript library. The `version`
        field must be an explicit full semver (e.g. "1.0.0"); it creates a new
        version and fails if that version already exists. The per-name latest is
        always the greatest stored semver.
      operationId: saveNumscript
      tags:
        - Numscript Library
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the numscript
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaveNumscriptRequest'
      responses:
        '201':
          description: Numscript saved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetNumscriptResponse'
          links:
            GetSavedNumscript:
              operationId: getNumscript
              parameters:
                ledgerName: '$request.path.ledgerName'
                name: '$response.body#/data/name'
        '204':
          description: Numscript saved (no content returned)
        '400':
          description: Bad request (version not an explicit full semver, parse error, etc.)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: |
            The version already exists (`NUMSCRIPT_VERSION_ALREADY_EXISTS`).
            Stored versions are immutable and cannot be overwritten.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/numscripts/{name}/versions:
    get:
      summary: List numscript versions
      description: |
        Returns the numscript's history: its current latest (greatest stored
        semver) and every stored version, highest-first.
      operationId: listNumscriptVersions
      tags:
        - Numscript Library
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the numscript
      responses:
        '200':
          description: Numscript versions retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNumscriptVersionsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/numscripts/{name}/usage:
    get:
      summary: Get template usage
      description: |
        Returns the invocation counter and last-used timestamp for a Numscript template. Values are
        materialised asynchronously by the `usagebuilder` subsystem from the FSM audit chain — expect
        up to one usagebuilder tick interval (~100 ms) of lag behind the live FSM. A never-invoked
        template returns a zero-valued response (not 404), so clients handle "never used" uniformly.

        On existing ledgers whose audit chain has been partially archived to cold storage, only
        invocations still reachable in Pebble are counted.
      operationId: getNumscriptUsage
      tags:
        - Numscript Library
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the numscript template
      responses:
        '200':
          description: Template usage retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetTemplateUsageResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/bulk:
    post:
      summary: Bulk operations
      description: |
        Execute multiple operations (create transactions, add metadata,
        revert transactions, delete metadata) in a single request. Requires
        `ledger:write` scope.

        The response status is decided in three tiers:

        1. **Request-level failures** (before per-element processing):
           malformed body → `400`, missing/insufficient scope → `401` or
           `403`, oversized body → `413`, leader unavailable → `503`.
           Returned unconditionally, `continueOnFailure` does not apply.
        2. **Infrastructure failures during processing** (transport
           timeout, cache-horizon exceeded, storage error): the overall
           status is `500`, regardless of `continueOnFailure`. Callers
           must retry the bulk — the top-level failure means the request
           did not complete deterministically.
        3. **Per-element errors during processing**: each element's error
           carries the HTTP status it would surface as a single request
           (via the same mapping `handleError` uses). The top-level
           status is the "worst" of them, with `continueOnFailure`
           applying only to per-element business failures (4xx):
           - Business failures (validation, not-found, conflict,
             precondition, permission — 4xx): rolled up per
             `continueOnFailure`. `true` → `200`; `false` (default) →
             the highest 4xx among failing elements (typically `400`).
           - Rate-limit failures (`KindResourceExhausted`, `429`):
             surface unconditionally as `429`.
           - Retryable infrastructure failures (leader loss,
             cache-horizon exceeded, `KindUnavailable`): surface as
             `503` with `Retry-After: 1`, regardless of
             `continueOnFailure`.
           - Non-retryable infrastructure failures (transport / storage
             errors, `KindInternal`, unclassified): surface as `500`.
           - Aborted elements (`context.Canceled` from the sequential
             runner after a prior failure): contribute nothing — the
             originating error drives the rollup.
      operationId: bulkOperations
      tags:
        - Transactions
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: continueOnFailure
          in: query
          schema:
            type: boolean
            default: false
          description: Continue processing remaining operations even if one fails
        - name: atomic
          in: query
          schema:
            type: boolean
            default: false
          description: Execute all operations atomically (not yet supported)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/BulkElement'
      responses:
        '200':
          description: Bulk operations completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkResponse'
        '400':
          description: Bulk operations completed with errors, or invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkResponse'
        '429':
          description: |
            A per-element error mapped to `KindResourceExhausted` (quota
            or rate-limit hit during processing). Surfaces unconditionally,
            regardless of `continueOnFailure`. Body carries the per-element
            details in `data`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '413':
          description: Request entity too large
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/prepared-queries:
    post:
      summary: Create a prepared query
      description: |
        Creates a new prepared query for a ledger. A prepared query defines a reusable
        filter that can be executed later with parameter substitution. Requires `ledger:QueryWrite` scope.
      operationId: createPreparedQuery
      tags:
        - Prepared Queries
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePreparedQueryRequest'
      responses:
        '204':
          description: Prepared query created successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    get:
      summary: List prepared queries
      description: Returns a list of all prepared queries for a ledger. Requires `ledger:QueryRead` scope.
      operationId: listPreparedQueries
      tags:
        - Prepared Queries
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      responses:
        '200':
          description: List of prepared queries retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListPreparedQueriesResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/prepared-queries/{queryName}:
    put:
      summary: Update a prepared query
      description: |
        Updates the filter of an existing prepared query. Requires `ledger:QueryWrite` scope.
      operationId: updatePreparedQuery
      tags:
        - Prepared Queries
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/QueryName'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePreparedQueryRequest'
      responses:
        '204':
          description: Prepared query updated successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    delete:
      summary: Delete a prepared query
      description: Deletes a prepared query from a ledger. Requires `ledger:QueryWrite` scope.
      operationId: deletePreparedQuery
      tags:
        - Prepared Queries
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/QueryName'
      responses:
        '204':
          description: Prepared query deleted successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/prepared-queries/{queryName}/execute:
    post:
      summary: Execute a prepared query
      description: |
        Executes a prepared query with optional parameter substitution. Supports LIST and
        AGGREGATE_VOLUMES modes with cursor pagination. Also accepts pageSize and cursor
        as query parameters. Requires `ledger:QueryRead` scope.
      operationId: executePreparedQuery
      tags:
        - Prepared Queries
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/QueryName'
        - name: pageSize
          in: query
          description: Maximum number of results per page (overrides body value)
          schema:
            type: integer
            format: uint32
        - name: cursor
          in: query
          description: Cursor for pagination (overrides body value)
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExecutePreparedQueryRequest'
      responses:
        '200':
          description: Query executed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExecutePreparedQueryResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/account-types:
    post:
      summary: Add an account type
      description: |
        Adds a new account type to a ledger's chart of accounts. Account types define
        address patterns for account validation. Requires `ledger:MetadataWrite` scope.
      operationId: addAccountType
      tags:
        - Account Types
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddAccountTypeRequest'
      responses:
        '201':
          description: Account type added successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    get:
      summary: List account types
      description: |
        Returns all account types defined on a ledger, sorted by name.
        Requires `ledger:AccountRead` scope.
      operationId: listAccountTypes
      tags:
        - Account Types
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      responses:
        '200':
          description: Account types retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAccountTypesResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/account-types/default-enforcement-mode:
    put:
      summary: Set default enforcement mode
      description: |
        Sets the default enforcement mode for accounts that do not match any defined
        account type. Must be STRICT or AUDIT. Requires `ledger:MetadataWrite` scope.
      operationId: setDefaultEnforcementMode
      tags:
        - Account Types
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetDefaultEnforcementModeRequest'
      responses:
        '204':
          description: Default enforcement mode set successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/{ledgerName}/account-types/{typeName}:
    get:
      summary: Get an account type
      description: Retrieves a specific account type by name from a ledger. Requires `ledger:AccountRead` scope.
      operationId: getAccountType
      tags:
        - Account Types
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/TypeName'
      responses:
        '200':
          description: Account type retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetAccountTypeResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
    delete:
      summary: Remove an account type
      description: Removes an account type from a ledger's chart of accounts. Requires `ledger:MetadataWrite` scope.
      operationId: removeAccountType
      tags:
        - Account Types
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - $ref: '#/components/parameters/TypeName'
      responses:
        '204':
          description: Account type removed successfully
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'

  /v3/_/logs/{sequence}:
    get:
      summary: Get a single system log by bucket-wide sequence
      description: |
        Returns the system log entry with the given bucket-wide sequence number.
        Falls back to cold storage when the log has been archived. Requires
        `ledger:OpsRead` scope.
      operationId: getLog
      tags:
        - Logs
      security:
        - BearerAuth: []
        - {}
      parameters:
        - name: sequence
          in: path
          required: true
          schema:
            type: integer
            format: uint64
      responses:
        '200':
          description: Log retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: System log entry (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/_/chapters:
    get:
      summary: List bucket chapters
      description: |
        Streams every chapter registered on the bucket, ordered by chapter id
        ascending. Requires `ledger:OpsRead` scope.

        Live, best-effort read: this route drains the full cursor and does not
        expose the gRPC read-consistency / pagination options
        (`checkpointId` / `minLogSequence` / bidirectional cursor). Use gRPC
        for consistency-bounded reads. (Tracked follow-up.)
      operationId: listChapters
      tags:
        - Ops
      security:
        - BearerAuth: []
        - {}
      responses:
        '200':
          description: Chapters retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: Chapter (protobuf JSON representation)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/_/chapter-schedule:
    get:
      summary: Get the chapter auto-rotation cron schedule
      description: |
        Returns the cluster-wide cron expression driving automatic chapter
        rotation. An empty string means auto-rotation is disabled. Requires
        `ledger:OpsRead` scope.
      operationId: getChapterSchedule
      tags:
        - Ops
      security:
        - BearerAuth: []
        - {}
      responses:
        '200':
          description: Chapter schedule retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      cron:
                        type: string
                        description: Cron expression, or empty when disabled.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/_/events-sinks:
    get:
      summary: List configured event sinks
      description: |
        Returns every event sink configured on the cluster. Requires
        `ledger:OpsRead` scope.
      operationId: getEventsSinks
      tags:
        - Ops
      security:
        - BearerAuth: []
        - {}
      responses:
        '200':
          description: Event sinks retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: >
                      Configured sinks together with their per-sink status, at
                      parity with the gRPC GetEventsSinks response.
                    properties:
                      sinks:
                        type: array
                        items:
                          type: object
                          description: SinkConfig (protobuf JSON representation)
                      sinkStatuses:
                        type: array
                        items:
                          type: object
                          description: >
                            SinkStatus (protobuf JSON representation): per-sink
                            error status and last-emitted cursor.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/_/signing-keys:
    get:
      summary: List registered Ed25519 signing keys
      description: |
        Streams every Ed25519 signing key registered on the cluster. Requires
        `ledger:OpsRead` scope.

        Live, best-effort read: this route drains the full cursor and does not
        expose the gRPC read-consistency / pagination options
        (`checkpointId` / `minLogSequence` / bidirectional cursor). Use gRPC
        for consistency-bounded reads. (Tracked follow-up.)
      operationId: listSigningKeys
      tags:
        - Ops
      security:
        - BearerAuth: []
        - {}
      responses:
        '200':
          description: Signing keys retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: SigningKey (protobuf JSON representation)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/_/indexes:
    get:
      summary: List bucket-wide indexes
      description: |
        Streams the bucket-scoped index registry. The `scope` query parameter
        selects the flavor: `all` (default, matches the ListIndexesRequest
        proto default) streams every entry across ledgers, skipping orphan
        entries whose owning ledger no longer exists; `bucket` surfaces only
        bucket-scoped entries (empty ledger, e.g. audit indexes). Use
        `GET /v3/{ledgerName}/indexes` for the per-ledger view. Requires
        `ledger:OpsRead` scope.
      operationId: listBucketIndexes
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - name: scope
          in: query
          schema:
            type: string
            enum: [all, bucket]
            default: all
      responses:
        '200':
          description: Indexes retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: Index (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/_/indexes/{canonicalId}:
    get:
      summary: Get a single bucket-scoped index registry entry
      description: |
        Returns the `Index` registry entry for a bucket-scoped index
        (`Ledger == ""`, e.g. audit indexes). Ledger-scoped lookups go
        through `GET /v3/{ledgerName}/indexes/{canonicalId}` — the two
        keyspaces are distinct so a bucket-scoped lookup with the same
        canonical will NotFound if only the ledger-scoped entry exists.
        Requires `ledger:OpsRead` scope.
      operationId: getBucketIndex
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - name: canonicalId
          in: path
          required: true
          schema:
            type: string
          description: Canonical form of the target IndexID.
      responses:
        '200':
          description: Index retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: Index (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/_/indexes/{canonicalId}/status:
    get:
      summary: Get the per-replica status of a single bucket-scoped index
      description: |
        Returns the `IndexEntry` (registry entry + backfill cursor +
        per-replica IndexVersionState) for a bucket-scoped index.
        Requires `ledger:OpsRead` scope.
      operationId: getBucketIndexEntryStatus
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - name: canonicalId
          in: path
          required: true
          schema:
            type: string
          description: Canonical form of the target IndexID.
      responses:
        '200':
          description: Index entry retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: IndexEntry (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/_/indexes/status:
    get:
      summary: Get aggregated index registry status
      description: |
        Returns the aggregated index registry status: per-index backfill cursor,
        per-replica IndexVersionState (current/pending), and cluster-wide
        counters (LastIndexedSequence, LastLogSequence, Lag, IndexFileSize).
        The optional `ledger` query parameter narrows the per-index entries to
        a single ledger; the aggregated counters always cover the bucket.
        Requires `ledger:OpsRead` scope.
      operationId: getIndexStatus
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - name: ledger
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Index status retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: GetIndexStatusResponse (protobuf JSON representation)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/{ledgerName}/indexes:
    get:
      summary: List indexes for a ledger
      description: |
        Streams the indexes registered on a specific ledger. Requires
        `ledger:LedgerRead` scope.
      operationId: listLedgerIndexes
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
      responses:
        '200':
          description: Indexes retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: Index (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      summary: Create an index on a ledger
      description: |
        Registers a new index on the ledger. The FSM begins the backfill on
        the next apply. The request body carries the target `IndexID` in
        canonical form (see `DELETE /v3/{ledgerName}/indexes/{canonicalId}`
        for the format). Requires `ledger:LedgerWrite` scope.
      operationId: createIndex
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: Idempotency-Key
          in: header
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id]
              properties:
                id:
                  type: string
                  description: |
                    Canonical form of the target IndexID. One of:
                      `tx_builtin:<TX_BUILTIN_INDEX_*>`
                      `log_builtin:<LOG_BUILTIN_INDEX_*>`
                      `account_builtin:<ACCT_BUILTIN_INDEX_*>`
                      `metadata:<TARGET_TYPE_*>:<key>`
                  example: metadata:TARGET_TYPE_ACCOUNT:color
      responses:
        '201':
          description: |
            Index creation queued. The body carries the canonical id of the
            created index (round-trip normalized), usable directly on the
            follow-up GET/DELETE `/v3/{ledgerName}/indexes/{canonicalId}` routes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateIndexResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/{ledgerName}/indexes/{canonicalId}:
    get:
      summary: Get a single index registry entry
      description: |
        Returns the `Index` registry entry (id, buildStatus, ledger,
        createdAt, lastBuiltAt, forwardEncodingVersion) for a single
        index, serialized in protobuf-JSON camelCase. Requires
        `ledger:LedgerRead` scope.
      operationId: getIndex
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: canonicalId
          in: path
          required: true
          schema:
            type: string
          description: Canonical form of the target IndexID.
      responses:
        '200':
          description: Index retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: Index (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      summary: Drop an index from a ledger
      description: |
        Removes an index from a ledger. The path parameter carries the target
        `IndexID` in canonical form (e.g. `metadata:TARGET_TYPE_ACCOUNT:color`,
        `tx_builtin:TX_BUILTIN_INDEX_TIMESTAMP`). Requires `ledger:LedgerWrite` scope.
      operationId: dropIndex
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: canonicalId
          in: path
          required: true
          schema:
            type: string
          description: Canonical form of the target IndexID.
        - name: Idempotency-Key
          in: header
          schema:
            type: string
      responses:
        '204':
          description: Index removed.
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v3/{ledgerName}/indexes/{canonicalId}/status:
    get:
      summary: Get the per-replica status of a single index
      description: |
        Returns the `IndexEntry` for a single index — the registry entry
        joined with the backfill cursor and the per-replica
        IndexVersionState (current + pending version). Use this to poll
        for backfill completion after a `POST /v3/{ledgerName}/indexes`.
        Requires `ledger:LedgerRead` scope.
      operationId: getIndexEntryStatus
      tags:
        - Indexes
      security:
        - BearerAuth: []
        - {}
      parameters:
        - $ref: '#/components/parameters/LedgerName'
        - name: canonicalId
          in: path
          required: true
          schema:
            type: string
          description: Canonical form of the target IndexID.
      responses:
        '200':
          description: Index entry retrieved successfully.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: IndexEntry (protobuf JSON representation)
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >
        JWT token issued by a trusted OIDC provider. Required when the server
        is started with `--auth-enabled`. Scopes: `ledger:read` (read operations),
        `ledger:write` (write operations), `ledger:admin` (cluster operations).

  parameters:
    LedgerName:
      name: ledgerName
      in: path
      required: true
      schema:
        type: string
      description: Name of the ledger

    TransactionId:
      name: transactionId
      in: path
      required: true
      schema:
        type: integer
        format: int64
      description: Transaction ID

    AccountAddress:
      name: address
      in: path
      required: true
      schema:
        type: string
      description: Account address

    MetadataKey:
      name: key
      in: path
      required: true
      schema:
        type: string
      description: Metadata key

    TargetType:
      name: targetType
      in: path
      required: true
      schema:
        type: string
        enum:
          - account
          - transaction
          - ledger
      description: Target type (account, transaction, or ledger)

    QueryName:
      name: queryName
      in: path
      required: true
      schema:
        type: string
      description: Prepared query name

    TypeName:
      name: typeName
      in: path
      required: true
      schema:
        type: string
      description: Account type name

    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      schema:
        type: string
        maxLength: 256
      description: Idempotency key for the request

    ListFilter:
      name: filter
      in: query
      required: false
      schema:
        type: string
      description: |
        A filter, accepted in EITHER of two interchangeable representations that
        parse to the same filter and are validated against the same per-target
        rules:

          - **Textual** — the human-readable filter grammar, e.g.
            `metadata[status] == "active"`, `address ^= "users:"`,
            `metadata[a] == "x" and metadata[b] == "y"`. Passed verbatim as the
            query-parameter value (URL-encoded).
          - **Structured** — the JSON QueryFilter DSL (see the `QueryFilter`
            schema: `$and`/`$or`/`$not` + `$match`/`$gt`/`$exists`/…). Passed by
            URL-encoding the JSON object as the query-parameter value.

        A condition that is not valid on the endpoint's target (e.g. a
        transaction-only condition on the accounts list) is rejected with 400.
        This generic filter is AND-combined with any convenience parameters the
        endpoint also exposes (e.g. the transactions `startDate`/`endDate`
        timestamp range). Selections that used to have dedicated aliases are
        expressed through this filter: an account address prefix via
        `filter=address ^= "users:"`, a transaction reference via
        `filter={"$match":{"reference":"..."}}` (URL-encoded).

        Date fields accept either an RFC3339 timestamp or raw Unix microseconds,
        in both representations: on transactions `timestamp` / `insertedAt` /
        `revertedAt` (e.g. textual `filter=timestamp >= "2023-11-14T22:13:20Z"`
        or structured `filter={"$gte":{"timestamp":"2023-11-14T22:13:20Z"}}`), and
        on logs `date` (e.g. `filter=date >= "2023-11-14T22:13:20Z"`). An RFC3339
        value before the Unix epoch (1970-01-01) is rejected with 400.

        Note: bare audit fields (`outcome`, `ledger`, `seq`, `proposal_id`,
        `timestamp`, `log_seq`, `caller_subject`, `order_type`) have no
        structured JSON form and must be passed in the textual representation.

  responses:
    ServiceUnavailable:
      description: Service unavailable (no leader available)
      headers:
        Retry-After:
          description: Number of seconds to wait before retrying the request
          schema:
            type: integer
            format: int32
            minimum: 1
            example: 1
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    Unauthorized:
      description: Authentication required (missing or invalid JWT token)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    Forbidden:
      description: Insufficient permissions (missing required scope)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    InternalServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    Conflict:
      description: Conflict (e.g. idempotency key conflict, ledger deleted)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    MethodNotAllowed:
      description: Method not allowed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    ChartViolation:
      type: object
      description: A chart of accounts violation reported in AUDIT enforcement mode
      required:
        - type
        - message
      properties:
        type:
          type: string
          description: The type of violation
        message:
          type: string
          description: Human-readable description of the violation
        account:
          type: string
          description: The account address that caused the violation

    MetadataValue:
      description: A typed metadata value (string, integer, boolean, or null). String values must not contain NUL bytes.
      nullable: true
      oneOf:
        - type: string
          pattern: '^[^\u0000]*$'
          description: Metadata string value without NUL bytes
        - type: integer
          format: int64
        - type: boolean

    MetadataFieldTypeCommand:
      type: object
      description: Declares the type of a metadata field, seeding a ledger's initial metadata schema.
      required:
        - targetType
        - key
        - type
      properties:
        targetType:
          type: string
          enum: [account, transaction, ledger]
          description: Which entity the metadata field applies to.
        key:
          type: string
          description: Metadata key.
          example: color
        type:
          type: string
          enum: [string, int64, bool, uint64, int8, int16, int32, uint8, uint16, uint32, datetime]
          description: Declared metadata value type.
          example: string

    CreateLedgerRequest:
      type: object
      properties:
        metadata:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/MetadataValue'
          description: Optional metadata for the ledger (supports typed values)
        mode:
          type: string
          enum:
            - NORMAL
            - MIRROR
          default: NORMAL
          description: Ledger mode. MIRROR creates a read-only ledger that syncs from an external v2 source.
        mirrorSource:
          $ref: '#/components/schemas/MirrorSourceConfig'
        defaultEnforcementMode:
          type: string
          enum:
            - STRICT
            - AUDIT
          description: Default chart-of-accounts enforcement for accounts that match no account type.
        initialSchema:
          type: array
          description: Metadata field type declarations to seed at creation time.
          items:
            $ref: '#/components/schemas/MetadataFieldTypeCommand'
        accountTypes:
          type: object
          description: Initial account types (name -> account type definition).
          additionalProperties:
            $ref: '#/components/schemas/AddAccountTypeRequest'
          example:
            user-checking:
              name: user-checking
              pattern: 'users:{id}:checking'
              persistence: EPHEMERAL
              segmentTypes:
                id:
                  type: uuid

    CreateLedgerResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/LedgerInfo'

    CreateIndexResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - id
          properties:
            id:
              type: string
              description: |
                Canonical form of the created IndexID (round-trip normalized),
                e.g. `metadata:TARGET_TYPE_ACCOUNT:color` or
                `tx_builtin:TX_BUILTIN_INDEX_TIMESTAMP`.
              example: metadata:TARGET_TYPE_ACCOUNT:color

    GetLedgerResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/LedgerInfo'

    ListAllLedgersResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/LedgerInfo'
          description: List of all ledgers in the cluster
          nullable: true

    LedgerInfo:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          description: Name of the ledger
        created_at:
          type: string
          format: date-time
          description: Creation timestamp (ISO 8601 format)
        deleted_at:
          type: string
          format: date-time
          description: Soft delete timestamp (null if not deleted)
          nullable: true
        metadata_schema:
          type: object
          description: Per-key metadata type declarations
          nullable: true
        mode:
          type: string
          enum:
            - NORMAL
            - MIRROR
          description: Ledger mode (NORMAL or MIRROR)
        mirror_source:
          $ref: '#/components/schemas/MirrorSourceConfig'
        mirror_sync_progress:
          $ref: '#/components/schemas/MirrorSyncProgress'
        account_types:
          type: object
          description: Per-type account address validation
          additionalProperties:
            type: object
            properties:
              name:
                type: string
              pattern:
                type: string
              persistence:
                type: string
                enum: [NORMAL, EPHEMERAL, TRANSIENT]
                description: |
                  NORMAL: fully persisted (default).
                  EPHEMERAL: persisted, purged when zero balance.
                  TRANSIENT: never persisted, must be zero at end of batch.
              segmentTypes:
                type: object
                description: Maps a pattern variable name to a type constraint.
                additionalProperties:
                  $ref: '#/components/schemas/SegmentType'
          nullable: true
        default_enforcement_mode:
          type: string
          description: Default enforcement for unmatched accounts

    MirrorSyncProgress:
      type: object
      description: Sync progress for MIRROR ledgers (populated at read time)
      required:
        - state
        - cursor
        - sourceLogCount
        - remainingLogs
      properties:
        state:
          type: string
          enum:
            - SYNCING
            - FOLLOWING
          description: Current sync state (SYNCING = catching up, FOLLOWING = up to date)
        cursor:
          type: integer
          format: uint64
          minimum: 0
          description: Last ingested v2 log ID
        sourceLogCount:
          type: integer
          format: uint64
          minimum: 0
          description: Latest known log ID in the v2 source
        remainingLogs:
          type: integer
          format: uint64
          minimum: 0
          description: Number of remaining logs to sync (sourceLogCount - cursor)
        error:
          $ref: '#/components/schemas/MirrorSyncError'

    MirrorSyncError:
      type: object
      description: Most recent mirror sync error (null if healthy)
      properties:
        message:
          type: string
          description: Error message
        occurredAt:
          type: string
          format: date-time
          description: When the error occurred

    CreateTransactionRequest:
      type: object
      description: |
        A transaction must declare exactly one content source: explicit
        `postings`, an inline `script`, or a `scriptReference`. The
        `postings` array cannot be combined with `script` or
        `scriptReference`. Idempotency is controlled exclusively via the
        `Idempotency-Key` HTTP header — there is no body-level key.
      properties:
        accountMetadata:
          type: object
          additionalProperties:
            type: object
            additionalProperties:
              $ref: '#/components/schemas/MetadataValue'
          description: Metadata for accounts involved in the transaction (supports typed values)
        timestamp:
          type: string
          format: date-time
          description: Transaction timestamp (ISO 8601 format). If not provided, current time is used.
        metadata:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/MetadataValue'
          description: Transaction metadata (supports typed values)
        reference:
          type: string
          description: Transaction reference
        postings:
          type: array
          items:
            $ref: '#/components/schemas/PostingRequest'
          description: List of postings for the transaction. Mutually exclusive with `script` and `scriptReference`.
        script:
          $ref: '#/components/schemas/Script'
          description: Inline Numscript to execute. Mutually exclusive with `postings` and `scriptReference`.
        scriptReference:
          $ref: '#/components/schemas/ScriptReference'
          description: Reference to a numscript saved in the ledger's library. Mutually exclusive with `postings` and `script`.

    PostingRequest:
      type: object
      required:
        - source
        - destination
        - amount
        - asset
      properties:
        source:
          type: string
          description: Source account address
        destination:
          type: string
          description: Destination account address
        amount:
          type: integer
          format: bigint
          description: Amount as a big integer
        asset:
          type: string
          description: Asset identifier
        color:
          type: string
          pattern: '^[A-Z]*$'
          maxLength: 32
          description: |
            Optional segregation key for "color of money". The empty string
            (or omitted field) means the uncolored bucket. Two postings on the
            same (account, asset) but different colors operate on strictly
            isolated balances. Color is constrained to `^[A-Z]*$`, capped at
            32 characters, and is immutable once carried by funds.

    Script:
      type: object
      required:
        - plain
      properties:
        plain:
          type: string
          description: The numscript source code
        vars:
          type: object
          additionalProperties:
            type: string
          description: Variables to pass to the script

    ScriptReference:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          description: Name of the numscript in the ledger's library
        version:
          type: string
          description: Optional semver constraint. Empty means latest.
        vars:
          type: object
          additionalProperties:
            type: string
          description: Variables to pass to the referenced script

    BulkElement:
      type: object
      required:
        - action
        - data
      properties:
        action:
          type: string
          enum:
            - CREATE_TRANSACTION
            - ADD_METADATA
            - REVERT_TRANSACTION
            - DELETE_METADATA
          description: |
            The type of operation to perform. The REST bulk endpoint accepts
            these four actions only; account-type actions
            (`ADD_ACCOUNT_TYPE` / `REMOVE_ACCOUNT_TYPE`) are gRPC-only and
            are rejected here.
        ik:
          type: string
          maxLength: 256
          description: Idempotency key for this operation
        data:
          oneOf:
            - $ref: '#/components/schemas/CreateTransactionRequest'
            - $ref: '#/components/schemas/AddMetadataRequest'
            - $ref: '#/components/schemas/RevertTransactionRequest'
            - $ref: '#/components/schemas/DeleteMetadataRequest'
          description: Operation-specific data
        skippableReasons:
          type: array
          items:
            type: string
            enum:
              - TRANSACTION_REFERENCE_CONFLICT
              - TRANSACTION_ALREADY_REVERTED
              - METADATA_NOT_FOUND
          description: |
            Opt-in continue-on-failure list for this entry. When the FSM
            would otherwise reject the underlying order with a business
            error whose typed reason matches one of these entries, the
            order is recorded as an `OrderSkippedLog` (no state mutation)
            and the bulk result's `data` surfaces the skip context.
            Reasons outside the per-action business whitelist (structural
            / internal reasons) are rejected at admission. Accepted
            reasons per action on this REST endpoint:
              - `CREATE_TRANSACTION` → `TRANSACTION_REFERENCE_CONFLICT`
              - `REVERT_TRANSACTION` → `TRANSACTION_ALREADY_REVERTED`
              - `DELETE_METADATA`    → `METADATA_NOT_FOUND`
            The FSM also supports skipping `ADD_ACCOUNT_TYPE`
            (`ACCOUNT_TYPE_ALREADY_EXISTS`) and `REMOVE_ACCOUNT_TYPE`
            (`ACCOUNT_TYPE_NOT_FOUND`), but those actions are **gRPC-only**:
            the REST bulk endpoint does not accept account-type actions (see
            the `action` enum above), so their reasons are not offered here.
            Empty (default) preserves the historical fail-on-error
            behaviour.

    AddMetadataRequest:
      type: object
      required:
        - targetType
        - targetId
        - metadata
      properties:
        targetType:
          type: string
          enum:
            - ACCOUNT
            - TRANSACTION
          description: Type of target (account or transaction)
        targetId:
          oneOf:
            - type: string
            - type: integer
          description: |
            ID of the target (account address or transaction ID). Required.
        metadata:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/MetadataValue'
          description: Metadata to add (supports typed values)

    RevertTransactionRequest:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64
          description: Transaction ID to revert. Required.
        force:
          type: boolean
          default: false
          description: Force revert even if accounts don't have sufficient funds
        atEffectiveDate:
          type: boolean
          default: false
          description: Set the timestamp of the reverted transaction to the original transaction timestamp
        metadata:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/MetadataValue'
          description: Metadata for the revert transaction (supports typed values)

    RevertTransactionRequestBody:
      type: object
      properties:
        force:
          type: boolean
          default: false
          description: Force revert even if accounts don't have sufficient funds
        atEffectiveDate:
          type: boolean
          default: false
          description: Set the timestamp of the reverted transaction to the original transaction timestamp
        metadata:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/MetadataValue'
          description: Metadata for the revert transaction (supports typed values)

    DeleteMetadataRequest:
      type: object
      required:
        - targetType
        - targetId
        - key
      properties:
        targetType:
          type: string
          enum:
            - ACCOUNT
            - TRANSACTION
          description: Type of target (account or transaction)
        targetId:
          oneOf:
            - type: string
            - type: integer
          description: |
            ID of the target (account address or transaction ID). Required.
        key:
          type: string
          description: Metadata key to delete

    BulkResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/BulkResult'
        errorCode:
          type: string
        errorMessage:
          type: string

    BulkResult:
      type: object
      properties:
        responseType:
          type: string
          description: Type of response (action name or ERROR)
        logID:
          type: integer
          format: int64
          description: Log ID of the applied operation (0 on error)
        errorCode:
          type: string
          description: Error code (present only on error)
        errorDescription:
          type: string
          description: Error description (present only on error)
        data:
          description: |
            Operation result data. Shape depends on the originating action:
              - `CREATE_TRANSACTION` success: a `TransactionResponse` — the
                created Transaction object.
              - Any skippable action skip (when the item opted into
                `skippableReasons` and the FSM matched one): an
                `OrderSkippedResponse` with `skipped: true`, `reason`, and
                reason-specific `context`. Emitted for every REST-supported
                skippable action — CREATE_TRANSACTION, REVERT_TRANSACTION,
                DELETE_METADATA — not just create.
              - Other successful non-create actions: `null`.
            Branch on `OrderSkippedResponse.skipped` to distinguish a skip
            from a successful transaction.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/TransactionResponse'
            - $ref: '#/components/schemas/OrderSkippedResponse'

    CreateTransactionResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          properties:
            transaction:
              $ref: '#/components/schemas/TransactionResponse'
            accountMetadata:
              type: object
              additionalProperties:
                type: object
              description: Metadata for accounts involved in the transaction
            chapterId:
              type: integer
              format: uint64
              description: Chapter that was OPEN when this transaction was created
            previousAccountMetadata:
              type: object
              additionalProperties:
                type: object
              description: Previous account metadata values (for index replay)
            warnings:
              type: array
              items:
                $ref: '#/components/schemas/ChartViolation'
              description: Chart of accounts violations (AUDIT mode only, empty in STRICT mode)

    OrderSkippedResponse:
      type: object
      description: |
        Shape returned in the bulk result's `data` field when a bulk entry
        opted into `skippableReasons` and the FSM matched one of the
        listed reasons (no state mutation occurred). `skipped` is always
        `true` on this shape — branch on it to distinguish from the
        normal Transaction data.
      required:
        - skipped
        - reason
      properties:
        skipped:
          type: boolean
          description: Always true on this shape.
        reason:
          type: string
          description: Typed reason that matched the skip (e.g. `TRANSACTION_REFERENCE_CONFLICT`). Same identifier the gRPC ErrorInfo.reason and the REST error responses use.
        context:
          type: object
          additionalProperties:
            type: string
          description: |
            Reason-specific correlator. For
            `TRANSACTION_REFERENCE_CONFLICT`, contains
            `reference` and `existingTransactionId`.

    GetTransactionResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - transaction
            - receipt
          properties:
            transaction:
              $ref: '#/components/schemas/TransactionResponse'
            receipt:
              type: string
              description: >-
                Signed verifiability receipt (JWT) for the transaction, mirroring
                the gRPC GetTransaction response. Reused from the controller read
                path — never recomputed at the HTTP layer — so it reflects the
                same committed state as the returned transaction. Always present;
                an empty string means the transaction legitimately has no receipt
                (e.g. a reversal, or a serving node that cannot sign).

    GetAccountResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/Account'

    GetLedgerStatsResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/LedgerStats'

    LedgerStats:
      type: object
      required:
        - transactionCount
        - volumeCount
        - referenceCount
        - postingCount
        - logCount
        - ephemeralEvictedCount
        - transientUsedCount
        - revertCount
        - numscriptExecutionCount
      properties:
        transactionCount:
          type: integer
          format: uint64
          minimum: 0
          description: Number of transactions in the ledger
        volumeCount:
          type: integer
          format: uint64
          minimum: 0
          description: Number of persisted (account, asset, color) volume rows
        referenceCount:
          type: integer
          format: uint64
          minimum: 0
          description: Number of transaction references registered in the ledger
        postingCount:
          type: integer
          format: uint64
          minimum: 0
          description: Total number of postings across all transactions
        logCount:
          type: integer
          format: uint64
          minimum: 0
          description: Number of log entries in the ledger
        ephemeralEvictedCount:
          type: integer
          format: uint64
          minimum: 0
          description: Number of ephemeral/draining volumes evicted from storage at commit
        transientUsedCount:
          type: integer
          format: uint64
          minimum: 0
          description: Number of transient volumes used across proposals
        revertCount:
          type: integer
          format: uint64
          minimum: 0
          description: Number of reverted transactions in the ledger
        numscriptExecutionCount:
          type: integer
          format: uint64
          minimum: 0
          description: Number of Numscript executions in the ledger

    Account:
      type: object
      required:
        - address
      properties:
        address:
          type: string
          description: Account address
        metadata:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/MetadataValue'
          description: Account metadata (typed values)
        volumes:
          type: array
          items:
            $ref: '#/components/schemas/AccountVolume'
          description: |
            Account volumes listed per (asset, color). Sorted by (asset, color)
            ascending for stable serialization. The empty color is the
            uncolored bucket; colored buckets are strictly isolated. Use the
            `collapseColors=true` query parameter on GET /accounts to sum
            every colored bucket of the same asset into a single uncolored
            entry.

    AccountVolume:
      type: object
      required:
        - asset
        - color
        - volumes
      properties:
        asset:
          type: string
          description: Asset identifier (e.g. "USD/2")
        color:
          type: string
          pattern: '^[A-Z]*$'
          maxLength: 32
          description: Color of the bucket. Empty string is the uncolored bucket.
        volumes:
          $ref: '#/components/schemas/VolumesWithBalance'

    VolumesWithBalance:
      type: object
      required:
        - input
        - output
        - balance
      properties:
        input:
          type: string
          description: Total amount received (as big integer string)
        output:
          type: string
          description: Total amount sent (as big integer string)
        balance:
          type: string
          description: Current balance (input - output, as big integer string)

    AggregateVolumesResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/AggregateVolumesData'

    AggregateVolumesData:
      type: object
      required:
        - volumes
      properties:
        volumes:
          type: array
          items:
            $ref: '#/components/schemas/AggregatedVolume'
          description: Flat aggregated volumes per asset
        groups:
          type: array
          items:
            $ref: '#/components/schemas/GroupedAggregateResult'
          description: Per-prefix aggregated volumes (present when groupByPrefixes is set)

    AggregatedVolume:
      type: object
      description: |
        Per-asset aggregated volume as emitted by `GET /v3/{ledgerName}/volumes`.
        Prepared-query `AGGREGATE_VOLUMES` responses (under the `aggregateResult`
        envelope of `ExecutePreparedQueryResponse`) use this exact shape:
        decimal-string amounts, a computed `balance`, and `color` always present.
      required:
        - asset
        - color
        - input
        - output
        - balance
      properties:
        asset:
          type: string
          description: Asset identifier (e.g. "USD/2")
        color:
          type: string
          pattern: '^[A-Z]*$'
          maxLength: 32
          description: |
            Color of the aggregated bucket. By default the response contains
            one AggregatedVolume per (asset, color) tuple. With
            `collapseColors=true`, every colored bucket of the same
            (asset, precision) is summed and the resulting entry carries
            color = "".
        input:
          type: string
          description: Total amount received (as big integer string)
        output:
          type: string
          description: Total amount sent (as big integer string)
        balance:
          type: string
          description: Net balance (input - output, as big integer string)

    GroupedAggregateResult:
      type: object
      required:
        - prefix
        - volumes
      properties:
        prefix:
          type: string
          description: Account prefix this group matches
        volumes:
          type: array
          items:
            $ref: '#/components/schemas/AggregatedVolume'
          description: Aggregated volumes for accounts matching this prefix

    TransactionResponse:
      type: object
      required:
        - id
      properties:
        postings:
          type: array
          items:
            $ref: '#/components/schemas/PostingResponse'
        metadata:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/MetadataValue'
        timestamp:
          type: string
          format: date-time
          description: Transaction timestamp (ISO 8601 format)
        reference:
          type: string
        id:
          type: integer
          format: int64
          description: Transaction ID (assigned by the system)
        reverted:
          type: boolean
          description: Whether this transaction has been reverted
        insertedAt:
          type: string
          format: date-time
          description: When the transaction was inserted into the ledger
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp
        revertedAt:
          type: string
          format: date-time
          description: When the transaction was reverted (null if not reverted)
          nullable: true
        revertedByTransactionId:
          type: integer
          format: int64
          description: ID of the transaction that reverted this one (absent when not reverted)
        revertsTransactionId:
          type: integer
          format: int64
          description: ID of the original transaction this transaction reverts (absent when this is not a revert)
        postCommitVolumes:
          $ref: '#/components/schemas/PostCommitVolumes'
          description: |
            Post-commit volumes for every (account, asset, color) tuple touched
            by this transaction, captured at the transaction's sequence. An
            immutable historical snapshot, present on every transaction; never
            recomputed from current balances.

    RevertedTransaction:
      type: object
      properties:
        revertedTransactionId:
          type: integer
          format: uint64
          description: ID of the original transaction that was reverted
        revertTransaction:
          $ref: '#/components/schemas/TransactionResponse'
          description: The new transaction that reverses the original transaction
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/ChartViolation'
          description: Chart of accounts violations (AUDIT mode only, empty in STRICT mode)

    RevertedTransactionResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/RevertedTransaction'

    PostingResponse:
      type: object
      required:
        - source
        - destination
        - amount
        - asset
        - color
      properties:
        source:
          type: string
        destination:
          type: string
        amount:
          type: integer
          format: bigint
          description: Amount as a big integer
        asset:
          type: string
        color:
          type: string
          pattern: '^[A-Z]*$'
          maxLength: 32
          description: |
            Segregation key for "color of money". The empty string means the
            uncolored bucket. Always present on responses (never omitted); two
            postings on the same (account, asset) but different colors operate
            on strictly isolated balances.

    LedgerClusterStateResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - state
            - localNode
            - innerState
          properties:
            state:
              type: string
              enum: [Leader, Follower, Candidate, PreCandidate, Unknown]
              description: Current state of the local node
            leader:
              type: integer
              format: int64
              minimum: 0
              description: ID of the current leader (0 if no leader)
            localNode:
              type: integer
              format: int64
              minimum: 0
              description: ID of the local node
            nodes:
              type: array
              items:
                $ref: '#/components/schemas/NodeInfo'
              description: List of all nodes in the cluster
            raftStatus:
              $ref: '#/components/schemas/RaftStatus'
              description: Complete Raft status information
            innerState:
              $ref: '#/components/schemas/LedgerState'
              description: State contained in the FSM

    SystemState:
      type: object
      required:
        - ledgers
      properties:
        ledgers:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/LedgerState'
          description: Map of ledger name to ledger state

    LedgerState:
      type: object
      required:
        - ledgerInfo
        - nextLogId
        - nextTransactionId
      properties:
        ledgerInfo:
          $ref: '#/components/schemas/LedgerInfo'
        nextLogId:
          type: integer
          format: uint64
          minimum: 0
          description: Next log ID to be assigned
        nextTransactionId:
          type: integer
          format: uint64
          minimum: 0
          description: Next transaction ID to be assigned

    NodeInfo:
      type: object
      required:
        - id
        - address
        - suffrage
      properties:
        id:
          type: integer
          format: int64
          minimum: 0
          description: Node ID
        address:
          type: string
          description: Node address
        suffrage:
          type: string
          enum: [Voter, Nonvoter, Learner]
          description: Node suffrage (Voter, Nonvoter, or Learner)

    ClusterState:
      type: object
      required:
        - state
        - localNode
      properties:
        state:
          type: string
          description: Current state of the Raft node (Leader, Follower, Candidate, PreCandidate, Unknown)
        leader:
          type: integer
          format: int64
          minimum: 0
          description: ID of the current leader (0 if no leader)
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/NodeInfo'
          description: List of all nodes in the cluster
        localNode:
          type: integer
          format: int64
          minimum: 0
          description: ID of the local node
        raftStatus:
          $ref: '#/components/schemas/RaftStatus'
          description: Complete Raft status information

    RaftStatus:
      type: object
      required:
        - state
        - term
        - leader
        - applied
        - commit
        - lastIndex
        - vote
      properties:
        state:
          type: string
          enum: [Leader, Follower, Candidate, PreCandidate, Unknown]
          description: Current Raft state
        term:
          type: integer
          format: int64
          minimum: 0
          description: Current term
        leader:
          type: integer
          format: int64
          minimum: 0
          description: ID of the current leader (0 if no leader)
        applied:
          type: integer
          format: int64
          minimum: 0
          description: Index of the last applied entry
        commit:
          type: integer
          format: int64
          minimum: 0
          description: Index of the last committed entry
        lastIndex:
          type: integer
          format: int64
          minimum: 0
          description: Index of the last entry in the log
        vote:
          type: integer
          format: int64
          minimum: 0
          description: ID of the node that received the vote (0 if no vote)
        progress:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/ProgressInfo'
          description: Progress information for each node in the cluster

    ProgressInfo:
      type: object
      required:
        - match
        - next
        - state
        - pendingSnapshot
        - recentActive
        - probeSent
        - isPaused
      properties:
        match:
          type: integer
          format: int64
          minimum: 0
          description: Match index - highest log entry known to be replicated to this node
        next:
          type: integer
          format: int64
          minimum: 0
          description: Next index - index of the next log entry to send to this node
        state:
          type: string
          enum: [Probe, Replicate, Snapshot, Unknown]
          description: State of the replication (Probe, Replicate, or Snapshot)
        pendingSnapshot:
          type: integer
          format: int64
          minimum: 0
          description: Index of pending snapshot (0 if none)
        recentActive:
          type: boolean
          description: Whether this node is recently active
        probeSent:
          type: boolean
          description: Whether probe message was sent (paused)
        isPaused:
          type: boolean
          description: Whether this node is paused

    InspectSummaryResponse:
      type: object
      required:
        - cardinality
        - entitiesWithKey
        - entitiesWithNull
      properties:
        cardinality:
          type: integer
          format: uint64
          description: Number of distinct values
        min:
          description: Minimum value (null if index is empty)
        max:
          description: Maximum value (null if index is empty)
        entitiesWithKey:
          type: integer
          format: uint64
          description: Number of entities that have this metadata key set (non-null)
        entitiesWithNull:
          type: integer
          format: uint64
          description: Number of entities with a null value for this key

    InspectDistinctValuesResponse:
      type: object
      required:
        - values
        - hasMore
      properties:
        values:
          type: array
          items: {}
          description: Distinct values in sorted order
        hasMore:
          type: boolean
          description: Whether more values are available
        nextCursor:
          type: string
          description: Pagination cursor for the next page

    InspectFacetsResponse:
      type: object
      required:
        - facets
        - hasMore
      properties:
        facets:
          type: array
          items:
            $ref: '#/components/schemas/InspectFacet'
          description: Facets sorted by value
        hasMore:
          type: boolean
          description: Whether more facets are available
        nextCursor:
          type: string
          description: Pagination cursor for the next page

    InspectFacet:
      type: object
      required:
        - value
        - count
      properties:
        value:
          description: The metadata value
        count:
          type: integer
          format: uint64
          description: Number of entities with this value

    MetadataSchemaStatusResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - accountFields
            - transactionFields
            - ledgerFields
          properties:
            accountFields:
              type: object
              additionalProperties:
                $ref: '#/components/schemas/MetadataFieldStatus'
              description: Account metadata field statuses
            transactionFields:
              type: object
              additionalProperties:
                $ref: '#/components/schemas/MetadataFieldStatus'
              description: Transaction metadata field statuses
            ledgerFields:
              type: object
              additionalProperties:
                $ref: '#/components/schemas/MetadataFieldStatus'
              description: Ledger metadata field statuses

    MetadataFieldStatus:
      type: object
      required:
        - declaredType
        - status
      properties:
        declaredType:
          type: string
          enum:
            - string
            - int64
            - bool
            - uint64
            - int8
            - int16
            - int32
            - uint8
            - uint16
            - uint32
            - datetime
          description: Declared type for this metadata field
        status:
          type: string
          enum:
            - COMPLETE
            - CONVERTING
          description: Conversion status for this field

    SetMetadataTypeRequest:
      type: object
      required:
        - type
      properties:
        type:
          type: string
          enum:
            - string
            - int64
            - bool
            - uint64
            - int8
            - int16
            - int32
            - uint8
            - uint16
            - uint32
            - datetime
          description: Metadata type to set for the field

    PostCommitVolumes:
      type: object
      description: |
        Post-commit volumes, keyed by account address. Each account maps to a
        flat array of per-(asset, color) volume tuples. The wire is a plain
        `{"addr": [{asset, color, input, output}]}` map — the proto
        `volumesByAccount` / `volumes` wrappers are flattened away by
        `PostCommitVolumes.MarshalJSON`. This replaces the pre-color
        `{"addr": {"asset": Volumes}}` map shape, which can no longer key a
        bucket uniquely once a color dimension exists.
      additionalProperties:
        type: array
        items:
          $ref: '#/components/schemas/VolumeEntry'

    VolumeEntry:
      type: object
      description: |
        A single flat post-commit volume tuple for one (asset, color) bucket.
        Input/output are flattened onto the tuple (not nested under a `volumes`
        key). The empty color is the uncolored bucket and is always emitted.
      required:
        - asset
        - color
        - input
        - output
      properties:
        asset:
          type: string
        color:
          type: string
          pattern: '^[A-Z]*$'
          maxLength: 32
          description: Empty string for the uncolored bucket; always emitted.
        input:
          type: string
          description: big.Int as string.
        output:
          type: string
          description: big.Int as string.

    Volumes:
      type: object
      required:
        - input
        - output
      properties:
        input:
          type: string
          description: Total amount received (as big integer string)
        output:
          type: string
          description: Total amount sent (as big integer string)

    MirrorSourceConfig:
      type: object
      description: Configuration for the mirror source (required when mode is MIRROR)
      required:
        - ledgerName
      properties:
        ledgerName:
          type: string
          description: Name of the source ledger in the v2 system
        type:
          type: string
          enum:
            - http
            - postgres
          default: http
          description: Source type (defaults to "http" if omitted)
        baseUrl:
          type: string
          description: Base URL of the v2 API (required for HTTP source)
        oauth2ClientId:
          type: string
          description: OAuth2 client ID for the v2 API (HTTP source only)
        oauth2ClientSecret:
          type: string
          description: OAuth2 client secret for the v2 API (HTTP source only)
        oauth2TokenEndpoint:
          type: string
          description: OAuth2 token endpoint URL (HTTP source only)
        oauth2Scopes:
          type: array
          items:
            type: string
          description: OAuth2 scopes (HTTP source only)
        dsn:
          type: string
          description: PostgreSQL connection string (required for PostgreSQL source)
        batchSize:
          type: integer
          minimum: 0
          default: 0
          description: Max logs per batch (0 = server default 100, capped by server's --mirror-max-batch-size)
        rewriteRules:
          type: array
          items:
            $ref: '#/components/schemas/MirrorRewriteRule'
          description: >
            Mirror rewrite rules applied, in order, to every mirror log entry as
            v2 logs are translated into v3 orders. Each rule is scoped to exactly
            one variant of the log's `data` oneof, with a CEL `match` predicate
            and a list of typed actions.

    MirrorRewriteRule:
      type: object
      description: >
        Transforms a mirror log entry during v2→v3 translation. Exactly one
        scope field (createdTransaction, revertedTransaction, savedMetadata,
        deletedMetadata, or anyVariant) must be set. That scope determines both
        the CEL type of `log` in the rule's `match` and the set of actions the
        rule may carry. When `stop` is true and the rule matches (scope +
        predicate), no further rules are evaluated. Rewriting is a
        deterministic, translation-time projection; the source v2 ledger is
        never modified.
      properties:
        createdTransaction:
          $ref: '#/components/schemas/CreatedTransactionRule'
        revertedTransaction:
          $ref: '#/components/schemas/RevertedTransactionRule'
        savedMetadata:
          $ref: '#/components/schemas/SavedMetadataRule'
        deletedMetadata:
          $ref: '#/components/schemas/DeletedMetadataRule'
        anyVariant:
          $ref: '#/components/schemas/AnyVariantRule'
        stop:
          type: boolean

    CreatedTransactionRule:
      type: object
      description: >
        Rule scoped to MirrorCreatedTransaction. Inside `match`, `log` is the
        created-transaction proto directly (e.g. `log.metadata["k"]`).
      properties:
        match:
          type: string
        actions:
          type: array
          items:
            $ref: '#/components/schemas/CreatedTransactionAction'

    RevertedTransactionRule:
      type: object
      properties:
        match:
          type: string
        actions:
          type: array
          items:
            $ref: '#/components/schemas/RevertedTransactionAction'

    SavedMetadataRule:
      type: object
      properties:
        match:
          type: string
        actions:
          type: array
          items:
            $ref: '#/components/schemas/SavedMetadataAction'

    DeletedMetadataRule:
      type: object
      properties:
        match:
          type: string
        actions:
          type: array
          items:
            $ref: '#/components/schemas/DeletedMetadataAction'

    AnyVariantRule:
      type: object
      description: >
        Rule scoped to every variant. `log` is the full MirrorLogEntry — use
        `has(log.<variant>)` guards to read variant-specific fields.
      properties:
        match:
          type: string
        actions:
          type: array
          items:
            $ref: '#/components/schemas/AnyVariantAction'

    CreatedTransactionAction:
      type: object
      description: Exactly one field must be set.
      properties:
        rewriteAddress:
          $ref: '#/components/schemas/RewriteAddressAction'
        setMetadata:
          $ref: '#/components/schemas/SetMetadataAction'
        deleteMetadata:
          $ref: '#/components/schemas/DeleteMetadataAction'
        setAccountMetadata:
          $ref: '#/components/schemas/SetAccountMetadataAction'
        deleteAccountMetadata:
          $ref: '#/components/schemas/DeleteAccountMetadataAction'
        setAccountMetadataFromAddress:
          $ref: '#/components/schemas/SetAccountMetadataFromAddressAction'
        drop:
          $ref: '#/components/schemas/DropAction'

    RevertedTransactionAction:
      type: object
      properties:
        rewriteAddress:
          $ref: '#/components/schemas/RewriteAddressAction'
        setMetadata:
          $ref: '#/components/schemas/SetMetadataAction'
        deleteMetadata:
          $ref: '#/components/schemas/DeleteMetadataAction'
        drop:
          $ref: '#/components/schemas/DropAction'

    SavedMetadataAction:
      type: object
      properties:
        rewriteAddress:
          $ref: '#/components/schemas/RewriteAddressAction'
        setMetadata:
          $ref: '#/components/schemas/SetMetadataAction'
        deleteMetadata:
          $ref: '#/components/schemas/DeleteMetadataAction'
        drop:
          $ref: '#/components/schemas/DropAction'

    DeletedMetadataAction:
      type: object
      properties:
        rewriteAddress:
          $ref: '#/components/schemas/RewriteAddressAction'
        drop:
          $ref: '#/components/schemas/DropAction'

    AnyVariantAction:
      type: object
      properties:
        rewriteAddress:
          $ref: '#/components/schemas/RewriteAddressAction'
        drop:
          $ref: '#/components/schemas/DropAction'

    RewriteAddressAction:
      type: object
      required: [pattern, replacement]
      properties:
        pattern:
          type: string
          description: RE2 pattern (validated at admission).
        replacement:
          type: string

    SetMetadataAction:
      type: object
      description: >
        `value` is a literal metadata string; `value_expr` is a CEL expression
        typed against the current variant and evaluated at commit time (e.g.
        `log.reference`). Exactly one of the two should be set; if neither is
        set the value defaults to the empty string. Optional `type` coerces
        the produced string into a typed MetadataValue via the platform
        conversion matrix — one of "string" (default), "int64", "bool",
        "uint64", "int8"/"int16"/"int32", "uint8"/"uint16"/"uint32",
        "datetime".
      required: [key]
      properties:
        key:
          type: string
        value:
          type: string
        value_expr:
          type: string
        type:
          type: string

    DeleteMetadataAction:
      type: object
      required: [key]
      properties:
        key:
          type: string

    SetAccountMetadataAction:
      type: object
      description: >
        See SetMetadataAction for the literal-vs-CEL and `type` semantics.
      required: [account, key]
      properties:
        account:
          type: string
        key:
          type: string
        value:
          type: string
        value_expr:
          type: string
        type:
          type: string

    DeleteAccountMetadataAction:
      type: object
      required: [account, key]
      properties:
        account:
          type: string
        key:
          type: string

    SetAccountMetadataFromAddressAction:
      type: object
      description: >
        For every posting address matching `pattern`, iterate every entry in
        `replacements` and set accountMetadata[address][key] to the RE2
        replacement of the address. Each replacement carries an optional
        metadata `type` token (same tokens as SetMetadataAction.type).
      required: [pattern, replacements]
      properties:
        pattern:
          type: string
        replacements:
          type: array
          items:
            $ref: '#/components/schemas/SetAccountMetadataFromAddressReplacement'

    SetAccountMetadataFromAddressReplacement:
      type: object
      required: [key, replacement]
      properties:
        key:
          type: string
        replacement:
          type: string
        type:
          type: string

    DropAction:
      type: object
      description: Turns the entry into a fill-gap that preserves log-ID and tx-ID continuity.

    SaveNumscriptRequest:
      type: object
      required:
        - content
        - version
      properties:
        content:
          type: string
          description: The numscript source code
        version:
          type: string
          description: |
            Explicit full semver (e.g. "1.0.0"). Appends a new immutable version;
            fails if that version already exists. "latest" and partial selectors
            are not accepted for save.
          example: "1.0.0"

    NumscriptInfo:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          description: Name of the numscript
        content:
          type: string
          description: The numscript source code
        version:
          type: string
          description: Semver version string (e.g. "1.0.0")
          example: "1.0.0"
        createdAt:
          type: string
          format: date-time
          description: Creation/update timestamp (ISO 8601 format)
        ledger:
          type: string
          description: Ledger name this numscript belongs to

    NumscriptVersionEntry:
      type: object
      required:
        - version
      properties:
        version:
          type: string
          description: Stored semver version string (e.g. "1.0.0")
          example: "1.0.0"
        createdAt:
          type: string
          format: date-time
          description: When this version was stored (ISO 8601 format)

    ListNumscriptVersionsResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          properties:
            latestVersion:
              type: string
              description: The current latest — the greatest stored semver
              example: "2.0.0"
            versions:
              type: array
              items:
                $ref: '#/components/schemas/NumscriptVersionEntry'
              description: Every stored version, highest-first
              nullable: true

    PromoteLedgerResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/LedgerInfo'

    GetNumscriptResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/NumscriptInfo'

    ListNumscriptsResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/NumscriptInfo'
          description: List of all numscripts
          nullable: true

    TemplateUsage:
      type: object
      required:
        - count
      properties:
        count:
          type: integer
          format: int64
          minimum: 0
          description: |
            Number of times the template has been invoked. `0` means never invoked (or the
            usagebuilder has not yet caught up to any invocation on this replica).
        lastUsed:
          type: string
          format: date-time
          nullable: true
          description: |
            Timestamp of the most recent invocation (ISO 8601). Absent when `count` is 0.

    GetTemplateUsageResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/TemplateUsage'

    ChartOfAccounts:
      type: object
      description: A suggested chart of accounts based on account analysis
      properties:
        accounts:
          type: array
          items:
            type: object
            properties:
              pattern:
                type: string
                description: Account name pattern
              description:
                type: string
                description: Suggested description for this account group

    AnalyzeAccountsResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          properties:
            patterns:
              type: array
              items:
                $ref: '#/components/schemas/AccountPattern'
            totalAccounts:
              type: integer
              format: int64

    AccountPattern:
      type: object
      properties:
        pattern:
          type: string
        accountCount:
          type: integer
          format: int64
        assets:
          type: array
          items:
            type: string
        metadataKeys:
          type: array
          items:
            type: string
        segments:
          type: array
          items:
            $ref: '#/components/schemas/PatternSegment'

    PatternSegment:
      type: object
      properties:
        position:
          type: integer
          format: int32
        type:
          type: string
          enum: [fixed, variable]
        fixedValue:
          type: string
        variableName:
          type: string
        inferredPattern:
          type: string
        uniqueValues:
          type: integer
          format: int64
        examples:
          type: array
          items:
            type: string

    AnalyzeTransactionsResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          properties:
            flowPatterns:
              type: array
              items:
                $ref: '#/components/schemas/FlowPattern'
            totalTransactions:
              type: integer
              format: int64
            totalReverted:
              type: integer
              format: int64

    FlowPattern:
      type: object
      properties:
        signature:
          type: string
          description: Canonical flow signature (e.g. "users:{id}:main->bank:fees[USD]")
        structure:
          type: string
          enum: [simple, multiSource, multiDestination, complex]
        transactionCount:
          type: integer
          format: int64
        postings:
          type: array
          items:
            $ref: '#/components/schemas/NormalizedPosting'
        temporal:
          $ref: '#/components/schemas/TemporalStats'
        volumeStats:
          type: array
          items:
            $ref: '#/components/schemas/AssetVolumeStats'
        metadataKeys:
          type: array
          items:
            type: string

    NormalizedPosting:
      type: object
      required:
        - sourcePattern
        - destinationPattern
        - asset
        - color
      properties:
        sourcePattern:
          type: string
        destinationPattern:
          type: string
        asset:
          type: string
        color:
          type: string
          pattern: '^[A-Z]*$'
          maxLength: 32
          description: |
            Color discriminates postings on the same (source, destination, asset)
            but different color buckets in the FSM. Always emitted (even empty)
            so clients can distinguish the uncolored bucket from patterns that
            differ only by color.

    TemporalStats:
      type: object
      properties:
        firstSeen:
          type: string
          format: date-time
        lastSeen:
          type: string
          format: date-time
        transactionsPerDay:
          type: number
          format: double
        peakHours:
          type: array
          items:
            $ref: '#/components/schemas/HourBucket'

    HourBucket:
      type: object
      properties:
        hour:
          type: integer
          format: int32
          minimum: 0
          maximum: 23
        count:
          type: integer
          format: int64

    AssetVolumeStats:
      type: object
      properties:
        asset:
          type: string
        totalVolume:
          type: string
        averageVolume:
          type: string
        minVolume:
          type: string
        maxVolume:
          type: string
        transactionCount:
          type: integer
          format: int64

    CreatePreparedQueryRequest:
      type: object
      required:
        - name
        - target
        - filter
      properties:
        name:
          type: string
          description: Unique name for the prepared query
        target:
          type: string
          enum:
            - ACCOUNTS
            - TRANSACTIONS
            - LOGS
          description: >-
            Target entity type. `LOGS` queries return log entries; the log-only
            filter fields (`logId`, `date`, `ledger`) are valid only when the
            target is `LOGS`.
        filter:
          $ref: '#/components/schemas/PreparedQueryFilterInput'

    UpdatePreparedQueryRequest:
      type: object
      required:
        - filter
      properties:
        filter:
          $ref: '#/components/schemas/PreparedQueryFilterInput'

    ExecutePreparedQueryRequest:
      type: object
      properties:
        parameters:
          type: object
          additionalProperties:
            type: string
          description: Parameter values for the query (substituted into filter placeholders)
        pageSize:
          type: integer
          format: int32
          description: Maximum number of results per page
        cursor:
          type: string
          description: Cursor for pagination (from previous response)
        minLogSequence:
          type: integer
          format: uint64
          description: Minimum log sequence the read index must have processed (freshness guarantee)
        mode:
          type: string
          enum:
            - LIST
            - AGGREGATE_VOLUMES
          description: Execution mode (default LIST)

    PreparedQueryResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/PreparedQuery'

    ListPreparedQueriesResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PreparedQuery'

    PreparedQuery:
      type: object
      required:
        - name
        - target
        - filter
      properties:
        name:
          type: string
          description: Query name
        target:
          type: string
          enum:
            - ACCOUNTS
            - TRANSACTIONS
            - LOGS
          description: >-
            Target entity type. `LOGS`, `ACCOUNTS` and `TRANSACTIONS` are all
            usable prepared-query targets (create, list/get and execute).
        filter:
          $ref: '#/components/schemas/QueryFilter'
        allowedModes:
          type: array
          items:
            type: string

    QueryFilter:
      type: object
      description: >-
        Recursive filter expression using the Formance query DSL (aligned with
        the v2 API). Each node is an object with **exactly one** top-level
        `$`-prefixed operator key.


        Combinators:

        - `{"$and": [QueryFilter, ...]}` — all sub-filters must match (non-empty).

        - `{"$or":  [QueryFilter, ...]}` — at least one sub-filter matches (non-empty).

        - `{"$not": QueryFilter}` — negates a single sub-filter.


        Leaf conditions are a single-key operator whose value is a
        `{ "<field>": <value> }` map with exactly one field:

        - `{"$match": {"<field>": <value>}}` — equality; for `address`/`source`/`destination`
          a trailing `:` means prefix match, otherwise exact.

        - `{"$gt"|"$gte"|"$lt"|"$lte": {"<field>": <value>}}` — range bound. A closed
          range is expressed as an `$and` of a lower and an upper bound on the same field.

        - `{"$exists": {"metadata": "<key>"}}` — the metadata key is present.

        - `{"$exists": {"asset": "<assetRef>"}}` — the account has ever held the asset
          (`assetRef` is `BASE` or `BASE/PRECISION`, e.g. `USD/2`). Requires a READY
          `account-asset` index.


        Supported fields: `metadata[<key>]`, `address`, `source`, `destination`,
        `reference`, `reverted` (bool), `ledger`, `id`, `timestamp`, `insertedAt`,
        `revertedAt`, `logId`, `date`. Not every field applies to every query target;
        a field that does not apply is rejected. The numeric built-ins
        (`id`, `timestamp`, `insertedAt`, `revertedAt`, `logId`, `date`) are
        **range-only** — use `$gt`/`$gte`/`$lt`/`$lte`; `$match` on them is rejected.
        The log-only fields (`logId`, `date`, `ledger`) are valid only when the
        prepared query targets `LOGS`; they are rejected on `ACCOUNTS` and
        `TRANSACTIONS` targets.


        A value is either a JSON literal or a parameter reference
        `{"$param": "<name>"}`, resolved at prepared-query execution time. A JSON
        `null` in value position is rejected. uint64 fields
        (`id`, `timestamp`, `insertedAt`, `revertedAt`, `logId`, `date`) are carried
        as decimal strings to stay lossless above 2^53. The date-semantic fields
        (`timestamp`, `insertedAt`, `revertedAt`, `date`) additionally accept an
        RFC3339 timestamp string (e.g. `{"$gte":{"date":"2023-11-14T22:13:20Z"}}`),
        coerced to the same Unix microseconds as the raw form; a value before the
        Unix epoch (1970-01-01) is rejected.
      minProperties: 1
      maxProperties: 1
      additionalProperties: true
      properties:
        $and:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/QueryFilter'
        $or:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/QueryFilter'
        $not:
          $ref: '#/components/schemas/QueryFilter'
        $match:
          type: object
          description: '`{ "<field>": <value> }` — exact/prefix/bool match.'
          minProperties: 1
          maxProperties: 1
          additionalProperties: true
        $gt:
          type: object
          description: '`{ "<field>": <value> }` — strictly greater than.'
          minProperties: 1
          maxProperties: 1
          additionalProperties: true
        $gte:
          type: object
          description: '`{ "<field>": <value> }` — greater than or equal.'
          minProperties: 1
          maxProperties: 1
          additionalProperties: true
        $lt:
          type: object
          description: '`{ "<field>": <value> }` — strictly less than.'
          minProperties: 1
          maxProperties: 1
          additionalProperties: true
        $lte:
          type: object
          description: '`{ "<field>": <value> }` — less than or equal.'
          minProperties: 1
          maxProperties: 1
          additionalProperties: true
        $exists:
          type: object
          description: >-
            `{ "metadata": "<key>" }` (metadata key present) or
            `{ "asset": "<assetRef>" }` (account has held the asset).
          minProperties: 1
          maxProperties: 1
          additionalProperties: true
        $in:
          type: object
          description: >-
            `{ "<field>": [<value>, ...] }` — value is one of a set. Reserved for
            the list endpoints; not accepted on prepared queries yet.
          minProperties: 1
          maxProperties: 1
          additionalProperties: true

    PreparedQueryFilterInput:
      description: >-
        A filter supplied to a prepared-query create/update request, accepted in
        EITHER of two interchangeable representations that parse to the same
        filter and are validated against the same per-target rules (EN-1511):


        - the structured JSON QueryFilter object (see `QueryFilter`); or

        - the textual filter grammar carried as a JSON string, e.g.
          `"metadata[status] == \"active\""`.


        Responses always echo the stored filter back in the structured
        `QueryFilter` form.
      oneOf:
        - $ref: '#/components/schemas/QueryFilter'
        - type: string
          description: Textual filter expression (filter grammar).

    ExecutePreparedQueryResponse:
      type: object
      description: |
        Result of executing a prepared query. Exactly one field is set,
        discriminated by the request execution `mode` (see
        `ExecutePreparedQueryRequest.mode`): `mode=LIST` returns `cursor`,
        `mode=AGGREGATE_VOLUMES` returns `aggregateResult`. (The query `target`
        — ACCOUNTS, TRANSACTIONS or LOGS — selects the row type inside the
        cursor, not which envelope field is set: `accountData`,
        `transactionData` or `logData` respectively. LOGS is a usable
        prepared-query target: `query.Execute` hydrates `logData` for it.) Both
        payloads use the same camelCase encoding as the equivalent non-prepared
        endpoints
        (decimal-string amounts, `color` always present on every volume row
        including the uncolored bucket).
      properties:
        cursor:
          type: object
          description: |
            Paged cursor, returned when the request `mode` is `LIST`. The row
            type follows the query `target`: `accountData` for ACCOUNTS,
            `transactionData` for TRANSACTIONS, `logData` for LOGS. Rows use the
            same camelCase shapes as the non-prepared list endpoints.
          properties:
            pageSize:
              type: integer
            hasMore:
              type: boolean
            previous:
              type: string
            next:
              type: string
            accountData:
              type: array
              items:
                $ref: '#/components/schemas/Account'
            transactionData:
              type: array
              items:
                $ref: '#/components/schemas/TransactionResponse'
            logData:
              type: array
              items:
                type: object
                description: System log entry (protobuf JSON representation)
        aggregateResult:
          $ref: '#/components/schemas/AggregateVolumesData'

    HealthResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - status
          properties:
            status:
              type: string
              description: Health status (e.g. "ok")

    ServerInfo:
      type: object
      required:
        - version
        - commit
        - buildDate
        - goVersion
      properties:
        version:
          type: string
          description: Server release version
        commit:
          type: string
          description: Git commit the binary was built from
        buildDate:
          type: string
          description: Build timestamp (RFC3339, "unknown" if unset)
        goVersion:
          type: string
          description: Go runtime version the binary was built with

    SegmentType:
      type: object
      description: |
        Constraint for a variable segment in an account type pattern. The
        `type` field discriminates the constraint kind; only `regex` carries a
        value.
      required:
        - type
      properties:
        type:
          type: string
          enum: [regex, uuid, uint64, bytes]
          description: |
            regex: custom regular expression (requires `regex`).
            uuid: RFC 4122 UUID format.
            uint64: decimal uint64.
            bytes: hex-encoded bytes (even length).
        regex:
          type: string
          description: Regular expression to match the segment against. Required when `type` is `regex`.
          example: '[A-Z]{2}[0-9]{14}'

    AddAccountTypeRequest:
      type: object
      required:
        - name
        - pattern
      properties:
        name:
          type: string
          description: Unique name for the account type
          example: user-checking
        pattern:
          type: string
          description: Account address pattern for validation. Variables use `{name}` segments.
          example: 'users:{id}:checking'
        persistence:
          type: string
          enum: [NORMAL, EPHEMERAL, TRANSIENT]
          default: NORMAL
          description: |
            How account volumes are stored.
            NORMAL: fully persisted (default).
            EPHEMERAL: persisted, purged when zero balance after commit.
            TRANSIENT: never persisted, must be zero at end of batch.
          example: EPHEMERAL
        segmentTypes:
          type: object
          description: Maps a pattern variable name to a type constraint.
          additionalProperties:
            $ref: '#/components/schemas/SegmentType'
          example:
            id:
              type: uuid

    AccountTypeInfo:
      type: object
      required:
        - name
        - pattern
        - persistence
      properties:
        name:
          type: string
          description: Account type name
        pattern:
          type: string
          description: Account address pattern
        persistence:
          type: string
          enum: [NORMAL, EPHEMERAL, TRANSIENT]
          description: How account volumes are stored (NORMAL, EPHEMERAL, or TRANSIENT)
        segmentTypes:
          type: object
          description: Maps a pattern variable name to a type constraint.
          additionalProperties:
            $ref: '#/components/schemas/SegmentType'

    ListAccountTypesResponse:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - types
          properties:
            types:
              type: array
              items:
                $ref: '#/components/schemas/AccountTypeInfo'
              description: List of account types sorted by name

    GetAccountTypeResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/AccountTypeInfo'

    SetDefaultEnforcementModeRequest:
      type: object
      required:
        - enforcementMode
      properties:
        enforcementMode:
          type: string
          enum:
            - STRICT
            - AUDIT
          description: Default enforcement mode for accounts not matching any account type

    ErrorResponse:
      type: object
      required:
        - errorCode
        - errorMessage
      properties:
        errorCode:
          type: string
          description: Error code identifier
        errorMessage:
          type: string
          description: Human-readable error message

    AuditEntry:
      type: object
      description: |
        A single audit trail entry for a proposal (success or failure). Exactly
        one of `success` / `failure` is set. `items` is populated only by the
        single-entry lookup, not by the list endpoint.
      properties:
        sequence:
          type: integer
          format: uint64
          description: Global audit sequence number
        timestamp:
          type: string
          format: date-time
          description: Apply timestamp (RFC3339)
        proposalId:
          type: integer
          format: uint64
          description: Identifier of the proposal that produced this entry
        success:
          $ref: '#/components/schemas/AuditSuccess'
        failure:
          $ref: '#/components/schemas/AuditFailure'
        orderCount:
          type: integer
          format: uint32
          description: Number of orders in the proposal
        items:
          type: array
          description: Per-order detail lines. Populated by the single-entry lookup; empty on list.
          items:
            $ref: '#/components/schemas/AuditItem'
        ledgers:
          type: array
          description: Distinct ledger names targeted by the proposal's orders
          items:
            type: string
        hash:
          type: string
          description: Hex-encoded batch integrity hash chaining audit entries
        hashVersion:
          type: integer
          format: uint32
          description: Hash algorithm version
        callerSnapshot:
          type: object
          description: Admission-time auth snapshot (protobuf JSON); absent when auth is disabled or for system proposals
        idempotency:
          type: object
          description: Batch identity / idempotency information (protobuf JSON)
        signature:
          type: object
          description: Client signature over the ApplyBatch (protobuf JSON); absent for unsigned batches

    AuditItem:
      type: object
      description: One order within a proposal.
      properties:
        orderIndex:
          type: integer
          format: uint32
          description: Zero-based position of this order within the proposal
        serializedOrder:
          type: string
          description: Hex-encoded deterministic serialized bytes of the order at apply time
        logSequence:
          type: integer
          format: uint64
          description: Sequence of the log produced by this order (0 for idempotent replay or failed proposals)

    AuditSuccess:
      type: object
      description: The inclusive log sequence range created by a successful proposal.
      properties:
        minLogSequence:
          type: integer
          format: uint64
        maxLogSequence:
          type: integer
          format: uint64

    AuditFailure:
      type: object
      description: The reason and context for a failed proposal.
      properties:
        reason:
          type: string
          description: Typed error reason identifier (e.g. ERROR_REASON_INSUFFICIENT_FUNDS)
        message:
          type: string
          description: Human-readable failure message
        context:
          type: object
          additionalProperties:
            type: string
          description: Additional structured context for the failure
