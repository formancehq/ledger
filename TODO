API:
* Checkpoint + exposed api to read at PIT
* Clean HTTP layer / GRPC proxy
* ACL system / authentication

Business:
* Chart of account
* Mirror mode
* Numscript
** Add typed variables

Tests:
* Convert K6 tests to GRPC
* Read heavy with K6

Refactor:
* Simplify processor
* Separate modules in application package
* Split admission
* Categorize raft commands (user commands vs system commands)
* Check all commands match a log type payload
* Decoupler wal write / state machine processing
* Leader reads
* Rework packages

Later:
* Events
** Sync S3 + parquet
* Cold storage:
** Add S3/Parquet support
* API
** Explore Checkpoint + exposed api to read at PIT
* Flow control