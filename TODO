API:
* Checkpoint + exposed api to read at PIT
* Eventual consistency (read your writes)
* Clean HTTP layer / GRPC proxy
* ACL system / authentication

Business:
* Flow control
* Mode not atomic
* Chart of account
* Mirror mode
* Numscript
** Add typed variables
** Add metadata typed variable draft
* Events
** Filter internal events
** Sync S3 + parquet
* Audit: enabled by the user
* Check/document idempotency

Tests:
* Convert K6 tests to GRPC
* Read heavy with K6
* Tests CLI

Cold storage:
* Add S3/Parquet support

Refactor:
* Simplify processor
* Separate modules in application package
* Split admission
* Categorize raft commands (user commands vs system commands)
* Check all commands match a log type payload
* Decoupler wal write / state machine processing