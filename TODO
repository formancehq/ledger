* Short circuit log ingestion
* Test https://connectrpc.com/
* Checkpoint + exposed api to read at PIT
* Flat buffer for the hot path
* Prevent configuration changes
* Doc performance (ulid, ordered keys, locality)
* Roll up of existing ledgers
* Chart of account pattern recognition
* Mirror mode
* Extract HTTP layer into a compat service?
* Checker add transactions
* Logs export CLI (filtrage par date/ledger)
* Sign logs hash with private key
* ACL system
* Mode not atomic
* Convert K6 tests to GRPC
* Eventual consistency (read your writes)
* Flow control
* refactor doc (dev, user)
* Automated periods
* Add hash consistency tests
* --bootstrap-from-backup
* Signing :
** when outputting the actions that were signed by a key, also output the key-id that validated the request
** have a list of keys
** in that list of keys, also map the key that has been used to register the key
** when revoking a key, potentially propose to revoke all keys that were registered using this key
** Sign responses too to avoid MITM
