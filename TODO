API:
* Checkpoint + exposed api to read at PIT
* Clean HTTP layer / GRPC proxy

Business:
* Progression of metadata type changes
* Add aggregations
* Query validations against types
* accounts migration
* Checkpoint when promoting ledger to be able to shutdown the mirrored ledger properly

Docs:
* Clean relying parties distinctions
* migrate existing clients
* Complexities

Tests:
* Convert K6 tests to GRPC
* Read heavy with K6
* Check schema eviction with time
* Auto port discovery
* Fetch all the production

Refactor:
* Simplify processor
* Split admission
* Check all commands match a log type payload
* Refactor Go-Libs
* Clean protobuf
* Prevent concurrent tests
* Index tracking
* Apply configuration schema
* Stop lifecycle
* bbolt fill percent
* bbolt compact
* configuration via command
* reorganize storage keys

Later:
* Events
** Sync S3 + parquet
* Cold storage:
** Add S3/Parquet support
* API
** Explore Checkpoint + exposed api to read at PIT
* Flow control
* Ledgerctl
** First usage detection and display
* Filtering
** Account types instead of segment filtering
* Numscript
** Add typed variables