API:
* Checkpoint + exposed api to read at PIT
* Clean HTTP layer / GRPC proxy

Business:
* Chart of account
* Mirror mode
* Numscript
** Add typed variables
* ledgerctl:
** backup progress
** watch command
** wrap long address in table views
** allow set metadata type on mirror mode
** Add self update
** First usage detection and display
* docs:
** migrate existing clients

Tests:
* Convert K6 tests to GRPC
* Read heavy with K6
* Check schema eviction with time

Refactor:
* Simplify processor
* Split admission
* Check all commands match a log type payload
* Log signal broadcast
* Simplify attributes system
* Smart compaction

Later:
* Events
** Sync S3 + parquet
* Cold storage:
** Add S3/Parquet support
* API
** Explore Checkpoint + exposed api to read at PIT
* Flow control